# JT 创作网站：手机上传、API 生图与玻璃界面交付

## 本次实现

- 视频工作流直接发送文件二进制，不再在手机上把整个文件复制并转换为 Base64。服务端采用 64 KiB 分块写入私有临时文件，再以带 Content-Length 的 multipart 流传给 RunningHub；保留旧 JSON 上传兼容性、60 MiB 限制、格式校验、会话绑定和额度限制。
- 选择文件后立即显示本地图片或可播放视频预览；显示真实上传百分比和“文件已传输，正在处理”。失败保留预览。更换素材立即移除旧凭证；上传未完成时阻止生成，过期响应不能覆盖新素材。切换工作流后恢复所选素材，并释放不再使用的 object URL。
- API 参数面板从生成按钮附近向上展开，无整页灰色遮罩。保留模型、比例、质量和画布适配；限制在当前视口可见空间内，支持内部滚动、Escape 关闭和焦点返回。
- 创作台、真人化、视频页统一玻璃材质。采用背景模糊、边缘高光与轻微边缘位移，保留输入区可读性；这是轻量视觉近似，不是物理光学折射模拟。手机操作区回到文档流，输入字号和触摸目标适配手机。
- 首页只携带最小启动配置，完整配置延迟加载。部署 HTML 为 440,790 字节，gzip 约 298 KB；完整配置 1,168,164 字节，gzip 约 557 KB，使用带哈希文件名和长期缓存。没有把文件体积下降等同于实测手机网络加载毫秒数。

## API 实现与本次真实验证

浏览器同源 `/api/generate` → Linux 面板任务 → SSH 反向隧道 → Windows 受限代理 → `POST https://dreamapi.club/v1/images/generations`。API Key 仅留在服务端，浏览器不直连中转站。新 Key 已用于隔离候选验证并写入正式服务的私密配置；已比对运行进程确实载入该 Key，不写入仓库、网页或交付包。

真实请求字段：`model=gpt-image-2.5-flare`、`quality=low`、`size=864x1536`、`n=1`、`output_format=png`。任务 `e41d1cb4bea5` 成功，约 237.9 秒，输出 PNG 1,612,639 字节。

必须区分请求参数和上游实际结果：本次上游返回 `medium`、`941x1672`，服务端以 contain 适配成最终 `864x1536`。因此最终尺寸能够固定，质量目前是请求偏好，不能保证中转站严格执行 low。结果记录同时保留实际质量、原始尺寸及适配标记。可选模型是 Image 2、2.5 Flare 和 2.5 Sunburst；本次只实测 Flare，不能用它替另外两个模型背书。

此前两个 Key 都曾收到 HTTP 403 `Image generation is not enabled for this group`。用户开启 Key 所属分组生图权限后，同一原生请求成功。该权限与账号 Hosted 桥接、模型白名单不同。另一次连接重置发生在工作站付费进程启动前，其持久 admission 记录未变化；保留独立失败证据，未把失败计作成功。

证据：`audit/dreamapi_native_20260917/verification.json`、`flare-low-9x16.png`、`upload-live-check.json`。RunningHub 真实上传已分别验证 PNG 和 MP4，均 HTTP 200；未启动付费视频生成。真实上传样本较小，不能代替实体手机弱网大文件实测。

## 源码与修改入口

| 内容 | 入口 |
| --- | --- |
| 上传接口、API 调度、缓存与任务恢复 | `server.py` |
| 视频上传、进度、预览与素材隔离 | `sources/video_business.js` |
| 共用玻璃材质与背景层 | `sources/workbench_glass.css` |
| 创作台及 API 设置面板 | `build_unified_three_styles.py` |
| 真人化和视频页面生成 | `build_realism_workbench.py`、`build_video_workbench.py` |
| 隔离单次原生生图验证 | `tools/prepare_dreamapi_native_canary.py` |
| 正式发布、备份及失败回滚 | `tools/deploy_realism_release.py` |

修改页面后运行三个 builder，生成 `static/index.html`、`promptgen.html`、`realism.html`、`video.html`。不要只修改生成文件。

背景扩展已预留 `--app-background-image`、`--app-background-size`、`--app-background-position`、`--app-background-opacity` 和 `--app-background-veil`。例如将第一个变量设置为 `url('/static/backgrounds/example.webp')` 即可接入背景。当前未新增背景上传或选择器；后续实现可直接复用这一层。

手机支持 JPG/PNG/WebP 图片和 MP4/MOV/WebM/AVI/MKV 容器，最大 60 MiB。HEIC 需先导出为 JPG；视频能否本地播放还取决于浏览器编码支持，MP4/H.264 兼容性更好。无法预览时显示编码说明，不以预览失败伪装上传成功。

## 验证与发布

脚本检查和 pytest 需分别通过发布工具运行，不能直接对整个 tests 目录执行 pytest，因为其中包括会主动退出的脚本契约检查。浏览器验证覆盖 390×844、1280×720、1440×900 的 API 面板，以及 320/390/820 宽度的其他工作台。核对裁切、横向溢出、焦点、素材替换和真实 DOM 行为。

正式发布只使用 `tools/deploy_realism_release.py --execute --allow-unauthenticated-public`，在干净的独立 worktree 执行。工具会校验证据、运行回归、暂停新任务、原子替换、读取线上文件校验并在失败时回滚。用户原有未提交文档没有纳入本次提交。原生验证绑定当前运行文件的 Git 内容摘要；修改这些文件后不能沿用旧成功证明。

工作站代理必须运行，电脑及隧道须保持在线；网页打开正常不代表上游随时可生成。任务使用同一个请求号恢复，连接中断时先查询状态，不盲目重复提交。新 Key 文件仅用于私密候选及服务端配置更新，交付包不含它。

2026-09-17 正式发布成功，运行版本为 `b34326267c704e060882b2ff29cfa1bf90a56ea0`，地址 http://8.210.125.65:8189 。发布工具完成 55 项脚本检查及 481 项 pytest 检查，所有发布文件回读摘要一致。发布事务已 committed、completed，任务入口已解除暂停。

上线后用隔离 Chromium 检查创作台、视频页、真人化页面的 390×844 和 1440×900 布局，六项均 HTTP 200、无横向溢出、无 JavaScript 页面异常；首页预览图加载成功，API 面板保持在按钮上方且在视口内。此检查禁止所有非 GET/HEAD 请求，没有再次付费生图或启动视频任务。可用 `python tools/verify_public_workbench.py` 重现，截图输出到 `evidence/public-workbench/`。

最终健康检查中 DreamAPI 已配置、工作站代理可达、运行契约一致、无不确定任务锁。独立的本地 ComfyUI 探测仍为 false；该状态不是 DreamAPI 失败，也没有将它宣称为正常。发布结果见 `audit/dreamapi_native_20260917/deployment.json`。本次没有实体手机弱网实测；后续遇到具体手机格式或网络失败，可据上传阶段和明确报错定位。
