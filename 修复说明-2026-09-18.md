# API 操作精简与共享限额修复

## 已查明的原因

线上账本中，2026-09-18 上午截图对应的四个任务均成功：云端 `7a54ec501d88`、API `39adc05b3598`、云端 `bab8edc60384`、API `0fa2a50af491`。原来的网站把同一浏览器的 API 和云端任务合并计入“每小时 4 次”，因此两次云端加两次 API 就会把两个入口一起限流。

`public billable task limit reached` 是网站自身的 HTTP 429，并非 DreamAPI 权限、模型不可用或 RunningHub 故障。这些被限流的请求不会进入上游生图。此次没有通过清空账本或删除任务来恢复。

发布前另查到 Windows 转发进程未运行，8198 端口拒绝连接，服务器健康检查的 DreamAPI egress 为 false。使用已有 `tools/start_comfy_watchdog.ps1` 恢复了原服务，随后 egress、运行契约和任务保护状态均恢复正常。现有启动目录中已有 `JT面板守护.vbs`；此次没有额外创建自动任务。电脑及该服务仍需在线，这与网站限额是两个独立问题。

## 修改逻辑

- 会话限额按后端分别计算：API 每小时 4 次，云端每小时 4 次。全站合计每小时 10 次、24 小时 30 次仍有效，因此全站额度耗尽时仍会同时限制两个付费入口。
- 限额按最近一小时／24 小时滑动计算；返回实际需要等待的秒数，不再错误地计算到下一个整点。错误带有 `error_code=billable_quota_exceeded`、`scope`、`backend`、`retry_after`，以及中文提示。
- 不把所有失败任务直接免计数：失败不等于上游没有计费，状态不明的任务也保留计数。重复请求使用原任务号，不产生额外任务。
- API 设置区仅承载参数；主操作区只有一个 API 生图按钮，设置按钮单独打开向上展开的面板。运行状态和已耗时间在生图按钮显示；去掉设置区、创作预览区的阶段卡片及重复状态。
- 上游没有提供实时百分比时，只显示已确认状态和耗时，不伪造百分比或剩余时间。失败保留原有结果，错误以文字呈现。
- 查询中断后按钮显示“继续查询”，恢复同一任务；提交响应不明确时保留原请求号及请求体，即使修改提示词或超过原先 30 分钟期限也不新建请求号，先确认原请求，避免重复计费。

## 验证范围

额度回归覆盖 API 达限后云端仍能提交、全站并发保护、幂等恢复、历史账本重新载入、日限额优先和准确到期边界。界面验证通过隔离浏览器和受控网络响应检查正常生成、拒绝请求、结果保留及手机面板布局。

候选运行版本 `ff87b67c983b1bdf1bbd24bb82c8d1b70b72a83c` 已通过一次真实 API 生图：任务 `1dcce15ef1f8`，26.8 秒，最终 864×1536 PNG，1,559,037 字节。原请求仍为 Flare / low / 9:16；上游实际返回 medium / 941×1672，画布适配行为不变。证据见 `audit/dreamapi_native_20260918/verification.json`。初次健康预检因转发进程不在线而停止，没有提交付费请求；恢复后只提交一次。

正式版本 `39ae7fb0b256a28855814469067c052072e098e8` 已于 2026-09-18 发布到 http://8.210.125.65:8189 。55 项脚本检查、489 项 pytest 测试通过；服务器文件回读摘要全部一致。发布事务已 committed、completed，任务入口已解除暂停。

上线后，创作台、视频页及真人化页面在 390×844、1440×900 两组尺寸下共六项浏览器检查通过。验证了唯一 API 生图按钮、移除内层提交和阶段卡片、无横向溢出、首页预览加载及无页面 JavaScript 异常。检查不提交生成任务。DreamAPI 健康检查正常，本地 ComfyUI 的独立探测仍为 false，没有将其宣称为恢复。

完整发布记录：`audit/dreamapi_native_20260918/deployment.json`。截图：`evidence/public-workbench-20260918/`。复核命令：`python tools/verify_public_workbench.py --output evidence/public-workbench-20260918`。
