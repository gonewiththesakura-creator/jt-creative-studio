# JT 灵感工作台｜完整项目审查

> 审查日期：2026-09-13（中国标准时间）
> 范围：代码、构建产物、后端合同、持久化、并发、发布器、生产只读状态、旧 Codex 任务“生成女孩写真图”、真人化/视频桌面与移动端布局、候选西雅图服务器
> 安全约定：本文不记录密码、私钥、API 凭据、Cookie 或供应商资产 ID。

## 1. 结论

项目可继续接手维护，但审查前并不满足稳妥发布标准。主要问题集中在三处：付费任务恢复与并发、持久化/匿名身份、前端异步状态与布局；旧发布器还存在测试对象漂移和停机窗口竞态。

本轮已修复所有确认的 P1 和可在当前合同内安全修复的 P2，并补齐回归测试。两个长期问题保留为显式风险：任务账本容量策略、供应商结果 URL 的条件性 SSRF。前者不能在没有收藏感知规则时直接清理，后者需要先收集 RunningHub 的真实 CDN 与重定向域名。

## 2. GPT Image 生图根因

用户没有官方 OpenAI API Key。旧任务也没有走官方 OpenAI API，而是：

1. 从本机 Codex 配置读取一个已经能访问 DreamAPI 的 Bearer 凭据；
2. 请求 `https://dreamapi.club/responses`；
3. 顶层模型固定为 `gpt-5.6-sol`；
4. 使用 hosted `image_generation` 工具调用 GPT Image 模型；
5. 解码 Base64 图片，再按请求画布裁切或留白。

初步诊断时，同一凭据曾从本机成功出图，而原香港面板服务器直接请求返回上游 502；这证明原生产网络路径确实有问题，因此增加受限本机出口是必要修复。发布后再次实测时，本机完全复刻旧成功请求和生产经隧道请求都返回 502，而同一 Bearer 的文本 Responses 与模型发现仍返回 200。由此可排除本项目参数、SSH 隧道、执行地域和“缺少官方 OpenAI API Key”，当前剩余根因是 DreamAPI 图片上游/适配器故障。

修复后的路径：

```text
浏览器 -> 香港面板 -> 127.0.0.1:8198 SSH反向隧道
       -> 本机 comfy_watchdog.py -> HTTPS DreamAPI /responses
```

边界：

- DreamAPI 凭据仍只保存在服务端环境中，不进入浏览器或 Git；
- 本机出口只接受固定请求形状、固定顶层模型、单个图片工具、模型/质量白名单和五种画布；
- 目标 URL 硬编码为 DreamAPI HTTPS `/responses`，禁止自动跟随重定向；
- 单出口一次只处理一个 API 生图请求；
- 本机服务只监听 loopback，服务器侧也只通过 loopback 反向隧道访问；
- 不自动重试不确定的生图结果，避免重复计费。

生产验收结果：

- `ff4b20b46edb`：`gpt-image-2` / low / 1:1，约 45.9 秒后 HTTP 502；
- `c587855be23a`：`gpt-image-2.5-flare` / low / 1:1，约 40.3 秒后 HTTP 502；
- 本机复刻旧任务的 hosted image request 同样为 502；
- 标准 Images 兼容入口返回 `Tool choice 'image_generation' not found in 'tools' parameter`，不能绕过故障；
- 未得到任何图片，已停止继续尝试，避免不确定计费和重复任务。

结论是“项目内调用链已修复并上线”，不是“GPT Image 已恢复”。恢复生图需要 DreamAPI 修复图片上游，或用户提供另一个可用的图片提供商/凭据。

## 3. 已修复问题

| 优先级 | 问题 | 处理结果 |
|---|---|---|
| P1 | RunningHub 轮询瞬时失败会永久标错已付费任务，重启后不恢复 | 保存 provider task ID；瞬时查询失败进入 `recovering` 并只恢复轮询，不重复提交 |
| P1 | 图片和视频通道可合计超过账户 2 个 RunningHub 并发 | 所有云端 RunningHub 任务统一计入总并发，`running/recovering` 都占槽 |
| P1 | `jobs.json` 损坏后静默清空，任务恢复、幂等和配额依据一起丢失 | 原子写入、fsync、上一版备份、读回校验；损坏原件另存，无有效备份时拒绝启动 |
| P1 | 创作台收藏的提示词、选项和种子无法回放 | 服务端保留并清洗创作台快照，仍剥离上传令牌和供应商私有字段 |
| P1 | 视频上传过程中切换工作流会把素材挂到错误工作流 | 上传绑定发起时工作流与版本；每个工作流独立保存素材状态 |
| P1 | 生产服务器原调用 DreamAPI 的网络路径不可靠 | 增加经过现有 8198 SSH 隧道的受限本机出口；传输路径已修复，供应商图片上游当前仍返回 502 |
| P1 | 发布可能部署未经测试的工作树字节，并在停机前接收新任务 | 发布锁定同一 Git HEAD；服务端原子排空后再停机；BaseException 也执行回滚 |
| P2 | 真人化/视频隐藏画廊仍强制 `display:grid`，与预览和任务区重叠 | 补 `hidden` 样式，结果和任务改为正常文档流；桌面/移动端重叠面积实测为 0 |
| P2 | 无扩展名的 `mp4/png` 结果被当成普通文件 | 同时识别标准 MIME、裸类型和 URL 后缀 |
| P2 | 创作台响应丢失并刷新后会换请求号，可能重复付费 | 请求指纹和 pending request ID 持久化；相同请求复用，明确 4xx 才清除 |
| P2 | 视频轮询可无限失败，历史/收藏永久停在“加载中” | 有界重试并显示可恢复状态；历史/收藏增加失败 UI |
| P2 | 视频上传宣称可拖入但不支持拖放和键盘 | 实现拖放、Enter/Space、可访问标签和状态播报 |
| P2 | 匿名 Cookie 24 小时后让用户丢失历史，旧 Cookie 服务端却永久有效 | 365 天服务端有效期、30 天刷新；旧格式只在限期迁移窗口接受 |
| P2 | 历史先全局截断再按用户过滤，其他用户会挤掉本人历史 | 先按 owner 过滤，再按页面范围排序截断 |
| P2 | 收藏上限跨用户执行，会删除其他用户收藏 | 按 owner 独立裁剪 |
| P2 | `/api/comfy/start` 和 LoRA 列表属于管理操作却公开，启动还有竞态 | 改为管理 Bearer；Comfy 启动 single-flight |
| P2 | 视频工作流公开数值参数缺上限 | 12 套工作流全部补 min/max/step；SCAIL 帧上限收紧到 3600 |
| P2 | 前端历史/收藏拼接服务端字符串到 `innerHTML` | 改用 `createElement`、`textContent` 与安全 URL 属性 |
| P2 | RunningHub 响应读取不在完整墙钟超时内 | 提交、查询、详情、上传统一使用有大小和墙钟截止的读取器 |
| P2 | 多个旧发布入口可绕过正式发布门 | 旧入口改为只提示迁移；唯一正式入口为 `tools/deploy_realism_release.py` |

## 4. API 生图交互

前端不再显示任意宽和高，只显示五个比例：

- `1:1`
- `2:3`
- `3:2`
- `9:16`（默认）
- `16:9`

服务端忽略客户端宽高并按比例映射可信尺寸。API 固定单张、Seed 标记为不适用；质量只表示请求偏好，结果卡同时显示上游实际模型、质量和尺寸。

## 5. 发布安全

正式发布器现在必须满足：

1. Git 工作树干净；
2. 脚本合同和 pytest 两条测试通道都非空，测试清单摘要与审查基线一致；
3. 测试前后 HEAD 不变，发布字节从该提交读取；
4. 公网健康接口连续三次确认 cloud/local/api 均空闲；
5. loopback 管理接口开启 admission drain，开启后再次确认无任务；
6. 隔离 watchdog 后设置“重启仍排空”，再停止服务和原子换包；旧服务首次迁移时由 systemd 强依赖的持久化 nftables 栅栏阻止任何公网启动窗口；
7. 公网页面、大响应和图片回读共享一个绝对墙钟 deadline，同时限制 Content-Length、无长度响应、gzip 线缆大小与解压大小；
8. 文件、systemd 状态、栅栏或重启验证任一步失败均回滚；
9. 回滚和 watchdog 恢复无法确认时保留发布锁并紧急停服，不能伪报成功。

## 6. 西雅图服务器可行度

候选机网络属于美国地址段，SSH 服务在 `49222` 可达，但当前只接受公钥，所给密码不能完成登录。未取得公钥登录前无法审计 CPU、内存、磁盘、swap、sing-box 配置和防火墙，因此不应把“端口可达”当成“可以迁移”。

已知条件下不建议整体迁移：

- 工作站到香港约 28–36ms，西雅图 TCP/SSH 约 162–328ms；
- 西雅图 1C/1GB 还运行 sing-box，处理最高约 96MiB Base64 响应时 OOM 风险明显；
- 本地 RTX 3060、8198/8199 反向隧道仍在国内工作站；
- DreamAPI 当前解析到香港面板 IP，迁移后会增加西雅图到香港的大响应往返；
- 任务账本、匿名 Cookie 签名和收藏媒体是有状态数据，不能只复制 Python/HTML。

建议：香港继续作为主站；西雅图先作为 DreamAPI 出口候选、无状态静态副本或冷备。若另选完整主站，优先实测香港、东京/大阪、新加坡、首尔；内存至少 2GB，建议 4GB，并用稳定 HTTPS 域名保持 Cookie 所有权。

## 7. 保留风险

| 风险 | 当前控制 | 后续完成标准 |
|---|---|---|
| 公网仍为 HTTP 且免登录 | 签名匿名 Cookie、全局/会话配额、管理接口独立 Bearer | HTTPS/WSS、Secure Cookie、稳定身份 |
| API 生图依赖本机与 SSH 隧道，且 DreamAPI 图片上游当前故障 | 不确定结果不重试，失败明确终止；文本接口单独验证 | 供应商恢复图片工具，或接入经真实媒体 E2E 验证的替代提供商 |
| DreamAPI 凭据不是官方 OpenAI API Key | 只在服务端和固定 HTTPS 目标使用 | 明确轮换流程，失效告警 |
| 任务账本无限增长 | 查询有范围，写入已原子化 | 收藏感知、运行中保护、可恢复窗口明确的清理策略 |
| 供应商 URL 条件性 SSRF | URL 语法清洗、浏览器私有字段剥离 | 收集真实 CDN/重定向合同后实施主机/IP allowlist |
| 历史维护脚本仍有密码登录与 AutoAddPolicy | 正式发布路径已钉住主机指纹，旧发布入口失效 | 逐步删除或改造成统一安全 SSH 库 |

## 8. 验收证据

- 三个权威页面构建器可确定性重建；
- Python 语法和 Git whitespace 检查通过；
- DreamAPI 本机出口相关专项测试通过；
- 浏览器 `1440×900` 与 `390×844` 实测：API 比例控件无任意宽高，真人化/视频画廊与任务区无重叠；
- 服务器经 8198 隧道发送合法结构、无效凭据的请求，由 DreamAPI 返回预期 401，证明完整网络路径可达且未触发生图；
- 正式全量发布门通过：55 个脚本合同与 34 个 pytest 文件均为 GREEN，发布返回 `PUBLIC_MARKERS_OK` 和 `REALISM_RELEASE_DEPLOY_OK`；
- 提交 `8c9a122` 已部署，生产健康检查确认 Comfy、本机出口、排空状态和三类 busy 状态正常；
- 两次单张低质量生产图片 E2E 都在供应商侧以 502 结束，本机完全复刻旧成功请求也为 502；没有生成媒体，故图片功能不能记为通过。
