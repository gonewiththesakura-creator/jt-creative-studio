# JT 灵感工作台｜完整项目交接

> 交接基线：2026-09-13 12:33（中国标准时间）  
> 本地仓库：`D:\LAN-Share\lora\_work\comfy_panel`  
> 分支：`feat/four-realism-workflows-cold-style`  
> 审查前代码 HEAD：`be16f8d`（本文提交后以最新 HEAD 为准）
> 公网地址：<http://8.210.125.65:8189>  
> 远端目录：`/home/admin/comfy-panel`  
> 当前状态：2026-09-13 完整审查修复已由提交 `8c9a122` 正式发布并通过发布门；界面与本机出口均已恢复，但 DreamAPI 图片上游当前仍返回 502。任务忙碌状态是动态值，接手时必须重新查询 `/api/health`。
> 凭据约定：本文不记录任何 API Key、SSH 密码、Cookie、访问令牌或私钥内容，统一视为 `[REDACTED]`。

**权威说明：** 本文与当前代码是接手依据。`运维文档_远程控制面板.md`、`冷脸萌提示词生成器_RunningHubAPI_项目交接_2026-08-25.md`仅为历史资料，其中“9个工作流、令牌鉴权、Ollama、旧deploy.py”等描述已经过时；发生冲突时以本文和当前代码为准。

---

## 0. 下一个 Agent 的最短接手路径

新 Agent 开始工作时，按这个顺序执行，不需要依赖历史聊天：

1. 阅读本文全部内容。
2. 执行：
   ```bash
   cd D:/LAN-Share/lora/_work/comfy_panel
   git status --short --branch
   git log -5 --oneline
   ```
3. 阅读权威文件：
   - `server.py`
   - `config.json`
   - `build_unified_three_styles.py`
   - `build_realism_workbench.py`
   - `build_video_workbench.py`
   - `sources/video_business.js`
   - `tools/deploy_realism_release.py`
4. 获取当前测试分类并运行正式双轨门：
   ```bash
   python -c "from tools.deploy_realism_release import classify_release_tests,run_release_tests; s,p=classify_release_tests(); print({'script_contracts':len(s),'pytest_files':len(p)}); run_release_tests(); print('FULL_RELEASE_TEST_GATE_OK')"
   ```
   当前基线：**55 个脚本合同 + 34 个 pytest 文件，0 个允许失败例外**。
5. 只读检查生产：
   ```bash
   python -c "import urllib.request,json; print(json.load(urllib.request.urlopen('http://8.210.125.65:8189/api/health',timeout=10)))"
   ```
6. 修改现有代码必须 TDD：先 RED，再最小 GREEN，再全门。
7. 正式发布只能使用：
   ```bash
   python tools/deploy_realism_release.py --execute --allow-unauthenticated-public
   ```
   仅当旧生产尚未安装独立发布令牌和 drain 合同时，首次迁移使用：
   ```bash
   python tools/deploy_realism_release.py --execute --allow-unauthenticated-public --bootstrap-drain-contract
   ```
   首次迁移成功后必须回到前一条标准命令，不能长期保留 bootstrap 参数。
   禁止使用旧 `tools/deploy.py` 代替正式发布器。
8. RunningHub 已取得 `taskId` 后只能轮询/下载，禁止改请求号自动重提。
9. 不要读取、打印或提交 `tools/creds.json`、私钥、环境文件或 `panel_data/`。

---

## 1. 产品定位与当前用户路径

JT 灵感工作台是一个公网、免登录的 AI 图片/视频控制台，目标用户不需要理解 ComfyUI 节点或供应商 API。

当前四个主要页面：

| 路由 | 页面 | 主要能力 |
|---|---|---|
| `/` | 创作台 | 10 种画风提示词随机器；RunningHub、本地 ComfyUI、DreamAPI 三种生图方式 |
| `/promptgen` | 创作台兼容入口 | 与 `/` 字节一致，浏览器会规范化到 `/` |
| `/realism` | 真人化/图片编辑 | 快速真人化、六套真人化工作流、全能修改图生图 |
| `/video` | 视频工作台 | 12 套视频工作流，分视频生成/视频编辑/动作迁移 |

历史兼容：

- `/realcomic` 重定向/兼容到真人化工作台。
- `/original-sketch`、`/original-graphic` 为历史兼容入口。
- `realism_3in1` 已从生产配置移除，只保留历史兼容识别。
- “三步绘画”和“铅绘逐步上色”已经取消，不要恢复。

### 1.1 当前结果交互

创作台、真人化、视频均使用统一方向：

- 单一大图/主视频；
- 下方横向缩略图；
- 左右翻页；
- 任务卡只显示状态，不再嵌入多个大媒体；
- 视频积累多个任务后不会把生成按钮挤出视口；
- 视频左侧参数区独立滚动；
- 图片/视频/ZIP 按真实类型展示；ZIP 仅下载卡，不塞进 `<img>`；
- 多输出视频带可信标签，例如“编辑后主视频”“原片 / 生成片对比”。

生成过程不显示数字百分比：

- 保留非数字液体进度动画；
- 保留“正在提交、正在生成、正在取回结果”等文字；
- HTML/CSS 中没有 `.liquid-percent`。

---

## 2. 系统架构

```text
浏览器（公网 HTTP）
  │
  ▼
8.210.125.65:8189
Ubuntu / systemd: comfy-panel
  ├─ Python stdlib HTTP 服务：server.py
  ├─ 静态页面：static/*.html
  ├─ 配置：config.json
  ├─ 持久任务/收藏/上传账本：panel_data/
  ├─ RunningHub OpenAPI v2
  ├─ DreamAPI/Sub2API: https://dreamapi.club/responses
  └─ SSH 反向隧道：127.0.0.1:8199 → Windows ComfyUI 127.0.0.1:8188
                                      │
                                      ▼
                         D:\ComfyUI_Mie\ComfyUI（RTX 3060 12GB）
```

### 2.1 服务器职责

公网服务器没有 GPU，不存放本地 ComfyUI 大模型，主要负责：

- 静态页面；
- 服务端可信配置；
- RunningHub/DreamAPI API 代理；
- 本地 ComfyUI 隧道代理；
- 异步任务状态机、幂等、历史、收藏；
- 结果下载和代理；
- 限流、会话隔离、健康检查；
- 原子发布和 watchdog。

### 2.2 Windows 本机职责

- 本地 ComfyUI：`D:\ComfyUI_Mie\ComfyUI`
- 默认接口：`127.0.0.1:8188`
- 面板守护：`comfy_watchdog.py`
- 一键启动：`启动面板.bat`
- 反向隧道：服务器 `127.0.0.1:8199` → 本机 `127.0.0.1:8188`
- 控制口：服务器 `127.0.0.1:8198` → 本机守护控制口

`启动面板隧道.bat` 是较早的单隧道脚本；日常优先使用 `启动面板.bat`。

---

## 3. 权威文件与生成关系

### 3.1 核心运行文件

| 文件 | 角色 |
|---|---|
| `server.py` | 单体后端：HTTP、会话、任务、RunningHub、DreamAPI、本地 ComfyUI、历史、收藏、下载、安全边界 |
| `config.json` | 21 个工作流的服务端可信目录、公开业务控件、节点映射、可信固定字段 |
| `templates/*.json` | 本地 ComfyUI API 工作流模板和历史模板 |
| `static/index.html` | 生产创作台 |
| `static/promptgen.html` | 与首页字节一致的兼容页 |
| `static/realism.html` | 真人化/图片编辑页 |
| `static/video.html` | 视频工作台 |

### 3.2 权威构建源

| 构建器/源 | 生成物 |
|---|---|
| `build_unified_three_styles.py` | `static/index.html`、`static/promptgen.html`、`sources/pool_union_manifest.json` |
| `build_realism_workbench.py` | `static/realism.html` |
| `build_video_workbench.py` + `sources/video_business.js` | `static/video.html` |
| `tools/build_preview_assets.py` | `static/previews/*` 与 manifest（仅在有明确源素材参数时使用） |

修改前端时必须修改构建源并重建，不能只手改 `static/*.html`。

重建命令：

```bash
python build_unified_three_styles.py
python build_realism_workbench.py
python build_video_workbench.py
```

随后至少运行：

```bash
node --check sources/video_business.js
python -m py_compile server.py build_unified_three_styles.py build_realism_workbench.py build_video_workbench.py
```

### 3.3 画风 Profile

- `sources/retro_manga_luxury_profile.json`
- `sources/style221_profile.json`
- `sources/style222_profile.json`
- `sources/hanmanga_profile.source.json`
- 铅绘/古风/冷脸萌等源页面与联合池文件位于 `sources/`

三个风格还带 `sparseRandomKeys`：复古、style221、style222 的“随机全部”只随机 9 个关键类别，其余归零，避免内容词压过画风核心。

---

## 4. 后端模块与关键逻辑

`server.py` 当前约 3600 行，主要区域如下。

### 4.1 启动配置

- `COMFY_URL`：本地/隧道 ComfyUI 地址；远端通常指向 `127.0.0.1:8199`。
- `CONTROL_URL`：本地守护控制口。
- `PANEL_PORT`：默认 `8189`。
- `PANEL_DIR`：运行数据目录，默认 `panel_data/`。
- `PANEL_TOKEN`：当前主要用于派生稳定会话签名秘密；公网 `_auth()` 仍按用户要求免登录。
- `PANEL_SESSION_SECRET`：可显式提供；否则从 `PANEL_TOKEN` 派生。
- `RUNNINGHUB_API_KEY`：RunningHub 服务端凭据。
- `DREAMAPI_KEY`：DreamAPI/Sub2API 凭据。
- `DREAMAPI_BASE_URL`：必须是根域 `https://dreamapi.club`，不能写成 `/v1`。
- `DREAMAPI_EGRESS_URL`：可选的本机出口；生产使用反向隧道内的 `http://127.0.0.1:8198/dreamapi/responses`，只允许 loopback HTTP 地址。
- `PANEL_RELEASE_TOKEN`：仅供 loopback 发布排空接口使用；由正式发布器一次性引导安装，不得复用 `PANEL_TOKEN`。

凭据只来自远端 systemd EnvironmentFile / 本机忽略文件，不得进入 Git。当前 API 生图不是官方 OpenAI API：它复用本机 Codex 配置中的 DreamAPI Bearer，服务端仍只将其发送到 DreamAPI 固定 HTTPS 端点。该 Bearer 当前可以完成模型发现和文本 Responses 请求，但图片工具上游不可用，不能把“凭据有效”写成“生图可用”。

### 4.2 持久化文件

远端根目录：`/home/admin/comfy-panel/panel_data/`

| 文件/目录 | 用途 |
|---|---|
| `panel_data/jobs.json` | 所有任务元数据与恢复状态 |
| `jobs/{job_id}/` | 本地归档图片或代理结果 |
| `panel_data/favorites.json` | 收藏元数据 |
| `favorites/` | 收藏媒体 |
| `panel_data/upload_capabilities.json` | 上传能力令牌哈希到账户资产映射；不保存明文令牌 |
| `panel_data/upload_usage.json` | 匿名上传次数/字节用量 |

不要把 `panel_data/` 提交到 Git；不要在文档/聊天输出其敏感内容。

### 4.3 任务状态机

通用状态：

- `running`
- `recovering`
- `done`
- `error`

常见 provider 状态：

- `SUBMITTING`
- `QUEUED`
- `RUNNING`
- `FINALIZING`
- `RESULT_TRANSFERRING` / `RESULT_DOWNLOADING`
- `DONE`
- `API_GENERATING`
- `API_DONE`
- `API_ERROR`
- `LOCAL_DONE`

关键原则：

1. 供应商接受任务后立刻持久化 `taskId`，再开始轮询。
2. 已有 `taskId` 或模糊提交结果时禁止自动重提。
3. 前端提交使用 `client_request_id`；同会话同有效 payload 去重。
4. 同会话同请求号但 payload 不同返回 409。
5. 不同签名会话使用相同请求号不会串任务。
6. 重启时有明确 `taskId` 的 RunningHub 任务进入 `recovering` 并恢复轮询。

关键函数：

- `normalize_client_request_id`
- `effective_request_sha256`
- `idempotency_decision`
- `persist_provider_task`
- `run_job`
- `resume_cloud_job`

### 4.4 匿名签名会话与所有权

Cookie：`jt_session`

- 内容为随机 session ID + HMAC 签名；
- `HttpOnly`、`SameSite=Strict`；
- 因当前是明文 HTTP，不能设置 `Secure`，否则浏览器不会发送；
- HTML 页面和 `/api/workflows` 会预置签名会话；
- 任务、历史、收藏、收藏媒体、本地预览按会话哈希隔离；
- 篡改/自造裸 session ID 不被接受。

注意：删除 Cookie 可获得新匿名身份，所以“每会话限额”不是身份认证；真正损失上限由全局限额兜底。若未来迁 HTTPS，应同时设置 `Secure` 并考虑稳定用户/设备身份。

### 4.5 上传能力令牌

浏览器上传后只得到不透明 `uploadToken`，不会获得 RunningHub `api/...` 文件名。

令牌绑定：

- 签名会话；
- workflow；
- input slot；
- 媒体类型；
- 24 小时 TTL。

服务端持久层只保存 token SHA-256。上传还会校验：

- 安全 basename；
- 扩展名；
- 图片魔数；
- 视频容器头；
- 工作流输入槽类型。

关键函数：

- `issue_upload_capability`
- `resolve_upload_capabilities`
- `validate_workflow_upload`
- `upload_media_content_type`
- `upload_bound_snapshot`

### 4.6 公网收费/上传限额

当前值：

| 类型 | 单会话 | 全局 |
|---|---:|---:|
| 收费任务/小时 | 4 | 10 |
| 收费任务/日 | — | 30 |
| 上传次数/小时 | 20 | 100 |
| 上传字节/小时 | 500 MiB | 2 GiB |

本地 ComfyUI 任务不计入云/API收费额度。已登记的新收费任务才计数；幂等重复不重复计数。

### 4.7 公开数据最小化

`public_workflow()` 只公开业务 UI 需要的字段，不公开：

- 工作流运行 ID；
- 编辑器来源 ID；
- 节点 ID/字段名；
- 模型文件名；
- 可信覆盖；
- provider 资产名；
- API Key。

`GET /api/job/{id}` 与 `/api/jobs`：

- 不公开顶层正向/负向 Prompt；
- 不公开 provider task ID、Comfy prompt ID、DreamAPI response ID；
- `selection_snapshot.params` 使用业务白名单；
- 顶层 `params` 只允许该任务 `rh_params` 中声明的键；
- 任意 `api_key/token/response_id/subject_prompt` 等旧数据不会回显；
- 收藏是 owner-only 路由，可以保留 Prompt/选项用于“套用/复制”。

### 4.8 HTTP 并发和大响应

- HTTP workers：32
- backlog：128
- 拒绝 workers：8
- 大响应并发：4
- 大请求并发：2
- socket/read/write 均有硬时限
- 预览优先、原图可后台归档/按需拉取
- RunningHub 下载支持中文 URL 安全编码，同时保留已有签名转义

---

## 5. 三种生图渠道

### 5.1 RunningHub 云端

创作台 `anima02` 使用 RunningHub workflow：`2091826879766933505`。

浏览器只提交：

- `style_id`
- `style_variant`
- Prompt/负向 Prompt
- 长宽、批量、高清、Seed

LoRA 文件、触发词、强度由 `server.py:STYLE_PRESETS` 决定，浏览器不能覆盖。

### 5.2 本地 ComfyUI

经 SSH 反向隧道连接本地 ComfyUI。主要流程：

1. `build_api()` 填模板占位符；
2. `/prompt` 提交；
3. WebSocket/历史轮询获取真实进度；
4. 优先生成轻量 WebP 预览；
5. 原图后台归档，或用户下载时按需获取。

### 5.3 DreamAPI/Sub2API

入口不是第三个主按钮，而是云端/本地按钮旁的独立“API 生图”入口，打开右侧专属抽屉。

侧栏控制：

- 模型：
  - `gpt-image-2`
  - `gpt-image-2.5-flare`
  - `gpt-image-2.5-sunburst`
- 比例：仅提供 `1:1`、`2:3`、`3:2`、`9:16`、`16:9`，默认 `9:16`
- 不向用户暴露独立宽/高；服务端将比例映射成受控画布并校正最终 PNG
- 质量：按模型白名单
- 适配：`cover` / `contain`
- 单次固定 1 张、关闭高清链
- Seed 不受支持，UI显示“不适用”

请求合同：

```text
POST https://dreamapi.club/responses
顶层模型：gpt-5.6-sol
stream: false
tools[0].type: image_generation
tools[0].action: generate
```

不要改为 `/v1/responses` 或 `/v1/images/generations`；历史实测这些路径不适用当前中转。

#### 2026-09-13 当前供应商故障

本轮已经修复香港面板到本机出口的网络路径，但发布后仍不能宣称图片 E2E 成功：

- 生产任务 `ff4b20b46edb`（`gpt-image-2`、low、1:1）约 45.9 秒后进入 `API_ERROR`，DreamAPI 返回 HTTP 502 `Upstream request failed`；
- 生产任务 `c587855be23a`（`gpt-image-2.5-flare`、low、1:1）约 40.3 秒后得到同样结果；
- 在本机完全复刻旧任务曾成功的 `gpt-5.6-sol + hosted image_generation + gpt-image-2 + 1024x1536 + low` 请求，同样返回 502；
- 同一 Bearer 的普通文本 Responses 请求返回 200，模型发现也返回 200，说明凭据与基础网关仍有效；
- DreamAPI 的标准 Images 适配器还会返回 `Tool choice 'image_generation' not found in 'tools' parameter`，不能作为绕行方案。

因此，当前剩余阻断在 DreamAPI 图片上游/适配器，而不是本项目参数、SSH 隧道、执行地域或缺少官方 OpenAI API Key。没有供应商恢复或替代凭据/提供商时，本地代码无法让其生成图片。不要自动重试，也不要为了“验收”继续提交可能计费的请求。

#### 长 Prompt 稳定性

原失败任务 `c684b06aeed7` 的正负 Prompt 都成功进入面板，但约 72 秒后上游 502。根因不是提示词没传，而是 Sub2API 图片桥接对长复杂输入不稳定。

当前策略：

- 面板接收正/负各最多 12,000 字符；
- 上游发送前按短语去重并压缩；
- **包括画布桥接说明在内，最终 `payload.input` ≤ 1600 字符**；
- 连续中文/英文无分隔文本使用字符预算兜底，不会压缩为空；
- 纯 ASCII/Unicode 标点主体在收费调用前拒绝；
- 异常终态为 `API_ERROR`；
- 不自动重试；
- 结果卡显示“请求质量”和“上游实际 model/quality/size”；上游可能覆盖质量。

关键函数：

- `compact_dreamapi_prompts`
- `build_dreamapi_input`
- `dreamapi_run_image`
- `_dreamapi_request_json`
- `_dreamapi_read_json`

真实长 Prompt 侧栏 E2E：

- 证据：`audit/dreamapi_sidebar_long_prompt_e2e.json`
- PNG：`audit/dreamapi_sidebar_long_prompt_20260913.png`
- 单次提交；3690 字符进入面板；上游正向 378 字符；输出 768×1024 PNG；请求 low、上游实际 medium。

---

## 6. 10 种创作画风

服务端权威映射在 `server.py:STYLE_PRESETS`。

| style_id | 展示 | Trigger | LoRA | 默认强度 |
|---|---|---|---|---|
| `cold` | 冷脸萌/3号画风 | `jt_style3_v2` | `05_style3_v2_step1600` + `04_style3_step800` | 0.7 / 0.6；另有 style-only 0.6 / 0 |
| `sketch` | 铅绘 | `jt_style1_v1` | `01_style1_step900` | 0.7 / 0.6 |
| `original_sketch` | 原始铅绘 | `jt_style1_v1` | `01_style1_step900` | 0.7 / 0.6 |
| `graphic` | 古风 | `jt_style2_v1` | `02_style2_step900` | 0.7 / 0.6 |
| `original_graphic` | 原始古风 | `jt_style2_v1` | `02_style2_step900` | 0.7 / 0.6 |
| `hanmanga` | 琉璃/韩漫向 | `jt_liulistyle_v1` | `08_liuli_style_v1_step600` | 0.7 / 0.6 |
| `nff` | NFF | `jt_nffstyle_v1` | `06_nff_style_v1_step2000` | 0.7 / 0.6 |
| `retro_manga_luxury` | 复古日漫奢华手绘 | `jt_style321_v1` | `09_style321_v1_step200` | 0.4 / 0 |
| `style221` | 板绘·半写实厚涂 | `zxqelun` | `11_style221_v1_step400` | 0.4 / 0 |
| `style222` | 铜版·雕刻排线厚涂 | `zxqavri` | `10_style222_v1_step400` | 0.4 / 0 |

注意：只有 style3 使用 v2；不要恢复错误映射 `jt_inkwash_v1`、`jt_softpaint_v1`、`jt_style2_v2`。

### 6.1 复古还原度修复

旧随机器把 31 类内容同时随机，随机内容约为固定画风核心的 1.93 倍，且核心位于后段，造成用户体感仅约 20% 还原。

当前修复：

- 加权核心媒介词前置；
- 拼接顺序：prefix → head → style core → 随机内容；
- 复古/221/222 使用稀疏随机 9 类；
- 全部 32 类选项仍保留，每类都有“无”；
- 画风核心与基础人物内容分层。

### 6.2 style221/style222 验证边界

- 本地正式 LoRA 与训练输出 SHA-256 一致；
- 云端各完成一次单次提交烟测；
- 证据：`audit/style221_222_panel_e2e_20260913.json`；
- 本地 4 候选 × 3 seed 共 12 图矩阵通过机械核验；
- 两者属于同一画风家族中的两种肖像倾向，跨题材区分性有限，不要夸大为高度稳定的完全独立风格。
- 现有烟测图手部/风格不够格，因此**没有发布正式预览图**；UI明确显示“暂无通过质量审核的预览”。

---

## 7. 21 个工作流总表

### 7.1 创作台

| ID | 名称 | Provider/类型 |
|---|---|---|
| `anima02` | Anima Base · 人物加画风（漫画剧情） | RunningHub / 本地 ComfyUI / DreamAPI 三通道 |

### 7.2 真人化与图片编辑（8 项）

| ID | 名称 | 类型/输入摘要 |
|---|---|---|
| `realcomic` | 快速真人化（原漫画转真人） | AI App；图+需求 |
| `realism_krea2` | Krea2 动漫转真人 | 图+指令+尺寸+Seed |
| `realism_2511` | 动漫转写实真人2511 | 图+指令+Seed |
| `realism_multisample` | 动漫转真人·多采超清 | 图+批量+Seed |
| `realism_qwen_zi` | Qwen+ZI动漫转真人 | 图+正负指令+尺寸 |
| `realism_4k_text` | 超写实4K文生图 | 文本+作者设计LoRA开关+尺寸+批量 |
| `realism_zi_flowmatch` | Z-Image+FlowMatch | 图+指令+作者设计LoRA组开关 |
| `qwen_image_edit_zip` | 全能修改图生图 | 图+指令+批量+Seed；结果可为ZIP |

### 7.3 视频工作台（12 项）

#### 视频生成（3）

- `h3_t2v_i2v`
- `h3_lightx2v`
- `h3_4step`

#### 视频编辑（4）

- `wardrobe`
- `h3_edit_n`
- `h3_edit_t3`
- `h3_edit_remove`

#### 动作迁移（5）

- `action`
- `scail2_plus`
- `scail2_multi`
- `wan_action_stabilized`
- `scail2_action_replace`

### 7.4 六套附件导入工作流的确定映射

| 业务ID | 编辑页ID | 实际运行workflow ID |
|---|---:|---:|
| `h3_edit_n` | `2098341535964643329` | `2098330246185926657` |
| `h3_edit_t3` | `2098341701604966401` | `2098330116945457154` |
| `h3_edit_remove` | `2098341695082573826` | `2097937340852727809` |
| `qwen_image_edit_zip` | `2098345504735592450` | `2097924547454918658` |
| `wan_action_stabilized` | `2098345513989357570` | `2097924526817800193` |
| `scail2_action_replace` | `2098345531529936898` | `2097870206308147201` |

这些映射来自登录Chrome下载来源、附件哈希、实时API图和生产账号只读schema交叉验证；禁止按消息顺序猜配。

六套工作流公开字段、来源哈希、实时schema和E2E门均锁定在：

- `config.json`
- `tests/fixtures/uploaded_workflow_provenance.json`
- `audit/uploaded_workflows_live_schema_20260912.json`
- `audit/uploaded_workflows_live_e2e_20260912.json`
- `tools/deploy_realism_release.py`

### 7.5 全部 Provider ID 索引（仅服务端维护使用）

这些ID不得通过公网 `/api/workflows` 暴露，也不得允许浏览器覆盖。

| 业务ID | 运行ID / AI App ID |
|---|---:|
| `anima02` | `2091826879766933505` |
| `h3_t2v_i2v` | `2093554209044480001` |
| `wardrobe` | `2093554049743339522` |
| `action` | `2093554104655167490` |
| `h3_lightx2v` | `2093554154237509633` |
| `h3_4step` | `2093554183483215873` |
| `realcomic` | `2025090022289973249` |
| `realism_krea2` | `2096139245572726785` |
| `realism_2511` | `2096138870382723074` |
| `realism_multisample` | `2096103615374155777` |
| `realism_qwen_zi` | `2096101840168747010` |
| `realism_4k_text` | `2095331229284470786` |
| `realism_zi_flowmatch` | `2096150319347859458` |
| `scail2_plus` | `2096841102812053505` |
| `scail2_multi` | `2096840691372924929` |
| `h3_edit_n` | `2098330246185926657` |
| `h3_edit_t3` | `2098330116945457154` |
| `h3_edit_remove` | `2097937340852727809` |
| `qwen_image_edit_zip` | `2097924547454918658` |
| `wan_action_stabilized` | `2097924526817800193` |
| `scail2_action_replace` | `2097870206308147201` |

---

## 8. RunningHub 通用接入方法

新增/修改 RunningHub 工作流时遵循：

1. 获取 editor JSON 与 API JSON；计算 SHA-256。
2. 不从文件名或消息顺序猜 workflow ID。
3. 用登录页面来源/下载记录或官方只读 schema 确认 editor ID → run ID。
4. 在 `config.json` 中只公开作者真实业务输入：
   - `rh_media`
   - `rh_params`
   - `params_defaults`
   - `section`
   - `rh_result_labels`
   - `fixed_features`
5. 节点 ID、fieldName、模型、VAE、Sampler、Scheduler、Steps、CFG、设备、LoRA等留在服务端可信配置。
6. 客户端上传必须走能力令牌，不得直接提交 provider `fileName`。
7. 使用通用路由，不新增每个工作流的专用后端：
   - `/api/workflow-upload`
   - `/api/workflow-generate`
   - `/api/upload`
   - `/api/video-generate`
8. 更新目录/发布门测试。
9. 真实E2E：固定 `client_request_id`，单次 POST；取得任务号后只轮询。
10. 检查结果容器、哈希、时长/尺寸、历史唯一和费用。

### 8.1 当前真实质量提醒

- `h3_edit_remove`：实测主结果存在手部模糊；功能/机械链路通过，UI有质量提醒。
- `scail2_action_replace`：角色替换质量任务在约 2.640 秒确认一帧双脸/躯干融合；功能链路通过，严格时序质量未全绿。
- `wan_action_stabilized`：最终4秒全身手脚挑战者通过24帧严格审查。
- T3/移除的第二输出是作者设计的原片/生成片对比，不是异常双画面。
- SCAIL2第二输出是输入/参考/结果对比，第一输出才是主视频。

---

## 9. HTTP 路由概要

### 9.1 健康和目录

- `GET /api/live`：轻量liveness
- `GET /api/health`：布尔健康、DreamAPI是否配置、三类任务槽忙碌状态
- `GET /api/comfy/status`
- `POST /api/comfy/start`
- `GET /api/workflows`：脱敏工作流目录并预置会话Cookie
- `GET /api/loras`：本地LoRA目录（维护用途）

### 9.2 任务

- `POST /api/generate`：创作台三通道
- `GET /api/job/{id}`：所属会话单任务
- `GET /api/jobs?scope=creator|realism|video[&style=...]`：所属会话历史
- `GET /api/image/{job}/{file}`
- `GET /api/local-preview/{job}/{file}`
- `GET /api/rh-preview/...`

### 9.3 真人化/通用工作流

- `POST /api/workflow-upload`
- `POST /api/workflow-generate`
- `POST /api/realcomic-upload`
- `POST /api/ai-app-generate`

### 9.4 视频

- `POST /api/upload`
- `POST /api/video-generate`

### 9.5 收藏

- `GET /api/favorites`
- `POST /api/favorites`
- `GET /api/favorite-image/{id}`
- `GET /api/favorite-preview/{id}`

所有任务、历史、收藏和媒体路由均有签名会话所有权检查。

---

## 10. 测试体系

该仓库混合两种测试：

1. 脚本合同：模块顶层执行并用 `sys.exit()` 返回；不能直接交给pytest收集。
2. pytest测试：正常 `test_*` 函数。

正式分类由 `classify_release_tests()` 用AST判断。

当前基线：

- 脚本合同：55
- pytest文件：34
- known baseline exceptions：0
- 发布文件：41

正确全门命令：

```bash
python -c "from tools.deploy_realism_release import classify_release_tests,run_release_tests; s,p=classify_release_tests(); print({'script_contracts':len(s),'pytest_files':len(p)}); run_release_tests(); print('FULL_RELEASE_TEST_GATE_OK')"
```

不要用普通 `pytest tests/` 代替全门；它会因脚本合同的 `SystemExit` 在collection阶段中断。

### 10.1 重要测试族

- DreamAPI：
  - `tests/test_dreamapi_creator_backend.py`
  - `tests/test_dreamapi_creator_http.py`
  - `tests/test_dreamapi_creator_ui.py`
  - `tests/test_dreamapi_health_http.py`
- 上传/隐私/会话/限额：
  - `tests/test_rh_upload_capabilities_http.py`
  - `tests/test_public_billable_quota_http.py`
- 通用RH：
  - `tests/test_generic_rh_workflow_http.py`
  - `tests/test_generic_rh_workflow_runtime.py`
- 画廊/UI：
  - `tests/test_media_gallery_ui.py`
  - `tests/test_video_catalog_sections_and_results.py`
- 新画风：
  - `tests/test_style221_222_and_retro_core.py`
  - `tests/test_style221_222_http.py`
- 发布：
  - `tests/test_realism_deploy_contract.py`
  - `tests/test_uploaded_workflow_release_gate.py`

---

## 11. 正式发布流程

### 11.1 预检

默认不修改远端：

```bash
python tools/deploy_realism_release.py --allow-unauthenticated-public
```

预期：所有业务 errors 为空，但因未传 `--execute` 返回非零。这是设计行为。

纯函数正式参数检查：

```bash
python -c "import json,pathlib; from tools.deploy_realism_release import preflight_decision; r=pathlib.Path('.'); d=preflight_decision(json.loads((r/'config.json').read_text(encoding='utf-8')),(r/'server.py').read_text(encoding='utf-8'),execute=True,allow_unauthenticated_public=True); print(d); assert d['ready']"
```

### 11.2 正式发布

前提：

- Git工作树干净；
- 所有变更已审查并提交；
- 双轨门通过；
- 生产无任务运行；
- 明确接受当前公网免登录风险。

命令：

```bash
python tools/deploy_realism_release.py --execute --allow-unauthenticated-public
```

旧生产第一次安装独立 `PANEL_RELEASE_TOKEN` 和 loopback drain 合同时，改用：

```bash
python tools/deploy_realism_release.py --execute --allow-unauthenticated-public --bootstrap-drain-contract
```

bootstrap 模式会先安装一个由 systemd 强依赖的持久化 nftables 入站栅栏；即使主机在迁移中重启，`comfy-panel` 也不能脱离栅栏对公网启动。首次迁移成功后禁止继续使用该参数。

成功标志必须同时出现：

```text
PUBLIC_MARKERS_OK
REALISM_RELEASE_DEPLOY_OK
```

### 11.3 原子发布事务

发布器执行：

1. 要求 Git 工作树干净，固定同一 HEAD，完整测试后从该提交读取不可变发布字节；
2. 固定 SSH 主机指纹验证，原子获取 `.release.lock`，创建 UUID 事务目录；
3. stage/backup/manifest 权限与 fsync，校验 manifest SHA-256，写入 `prepared` phase；
4. 公网连续三次稳定空闲后，通过独立 loopback Bearer 原子开启 admission drain；
5. 停用 watchdog timer/service，安装并验证“面板一启动即 drain”的持久化 drop-in；
6. bootstrap 首迁时先建立 systemd 强依赖链接和持久化 nftables 入站栅栏，再安装 drain 合同；
7. loopback 再连续三次确认空闲，停止面板并整组切换文件，逐字节读回；
8. 在 drain 状态启动新面板，验证健康、管理合同和精确字节；
9. bootstrap 首迁移除依赖链接并确认面板仍 active，随后关闭并清理栅栏；
10. 在一个绝对墙钟 deadline 内验证公网主页、真人化页、大响应、creator、preview 与 marker；
11. 安装并验证 watchdog 的有效 unit/timer 合同，再恢复 timer；
12. 写入 `committed` phase，清理事务并释放锁。

watchdog service是oneshot `static`单元。安全隔离实际状态允许：

- timer：inactive + disabled/masked
- service：inactive + static/masked

切换前隔离失败会恢复原watchdog状态。切换后异常若二次隔离、phase或回滚失败，会设置`rollback_uncertain`并保留锁；不能自动继续下一次发布。

### 11.4 发布后检查

```bash
python -c "import urllib.request,json; print(json.load(urllib.request.urlopen('http://8.210.125.65:8189/api/health',timeout=10)))"
```

远端预期：

- `comfy-panel`: active
- `comfy-panel-watchdog.timer`: active / enabled
- `comfy-panel-watchdog.service`: static（空闲时inactive正常）
- bootstrap fence 的 unit、nft 配置、Requires 链接和 nft table：全部不存在
- `.release.lock`: 不存在

---

## 12. 审计证据

正式发布门读取的主要证据：

- `audit/private_realism_workflows/e2e_manifest.json`
- `audit/scail2_video_e2e.json`
- `audit/retro_manga_panel_e2e_cloud_w04.json`
- `audit/dreamapi_creator_live_e2e.json`
- `audit/dreamapi_sidebar_long_prompt_e2e.json`
- `audit/dreamapi_sidebar_long_prompt_20260913.png`
- `audit/style221_222_panel_e2e_20260913.json`
- `audit/uploaded_workflows_live_schema_20260912.json`
- `audit/uploaded_workflows_live_e2e_20260912.json`
- `tests/fixtures/uploaded_workflow_provenance.json`

边界：这些是冻结声明 + 本地一致性证据，不是供应商数字签名的独立attestation。不要夸大。

`audit/retro_manga_cloud_w04_32421.png` 本机可能存在但被忽略；当前发布器不读取它。复古门读取已跟踪JSON并锁定尺寸/字节/SHA-256/任务/费用/视觉分类。

---

## 13. 当前生产状态

提交 `8c9a122` 正式发布后的只读回读如下。这仍仅是动态快照，不是永久状态：

```json
{
  "ok": true,
  "comfy": true,
  "local_comfy_ok": true,
  "dreamapi_configured": true,
  "dreamapi_workstation_egress": true,
  "draining": false,
  "cloud_busy": false,
  "local_busy": false,
  "api_busy": false
}
```

接手时务必重新请求 `/api/health`。如果任一 busy 字段为 `true`，不要重启、发布或提交测试任务，也不要读取/干预用户任务内容；等待现有任务自然完成。

公网：

- 21个工作流；
- 12个视频工作流；
- 10种画风；
- API侧栏存在；
- 创作/真人化/视频均是单主媒体画廊；
- 数字进度标记为0；
- style221/style222按画风筛选历史正常；
- 两套新画风无合格预览时显示明确说明。

最近关键提交：

- `8c9a122 [verified] harden panel and restore image API egress`
- `67f696f [verified] minimize public workflow task parameters`
- `c6eddc9 [verified] complete new style history and preview states`
- `53cb3ea [verified] remove numeric generation progress labels`
- `409ecbe [verified] handle static watchdog isolation safely`
- `27b9225 [verified] add media galleries API drawer and new styles`
- `9d644f9 [verified] harden release recovery durability`
- `1df8e50 [verified] fail closed on watchdog isolation`
- `ec82fa1 [verified] ship DreamAPI and curated RunningHub workflows`

---

## 14. 服务器迁移判断

候选机：美国西雅图 `104.254.211.5:49222`，1核/1GB，已运行 sing-box。

结论：**不要整体迁移控制台。**

理由：

- 从当前工作站实测现有香港入口约 28–36ms、美国西雅图约 162–328ms；ICMP 均值约 36ms 对 188ms；
- 美国机跨境RTT约 5.2 倍；
- 1核/1GB同时跑代理后余量很低；
- Python异步任务、上传、账本和代理会增加swap及尾延迟；
- `dreamapi.club` 当前解析到现有香港主机，整体迁移反而会给最高 96MiB 的 Base64 图片响应增加西雅图↔香港往返；
- 当前主机已经稳定承载生产，Windows RTX 3060 与反向隧道也仍在国内工作站。

推荐：

- `8.210.125.65`继续承载API、任务状态、历史和动态代理；
- 美国机继续专用sing-box；
- 若将来利用美国机，优先做 DreamAPI 出口验证、无状态静态副本或冷备，不直接迁移主站；
- 候选机SSH当前只接受公钥，提供的密码不能登录；先通过服务商控制台安装公钥并轮换聊天中暴露过的实例密码；
- 若以后另选主站地域，优先顺序是香港，其次以实测选择东京/大阪、新加坡、首尔；必须同时测用户访问、RunningHub、DreamAPI 三条路径；
- 若确需承载完整主站，内存至少 2GB，建议 4GB，并先完成 HTTPS 域名、数据原子迁移和 8198/8199 隧道切换演练。

---

## 15. 已知边界与不可隐瞒事项

1. **公网仍是HTTP免登录。** 这是用户明确暂时接受的产品决定。
2. 签名Cookie防伪造但不能防明文链路窃听；删除Cookie可重置匿名身份。
3. 全局限额能封顶损失，但不等于身份认证。
4. 真正解决需要HTTPS/WSS、Secure Cookie和稳定用户/设备身份。
5. style221/style222区分性有限，暂无合格正式预览。
6. SCAIL2角色替换有已知单帧重影风险。
7. H3移除元素有手部模糊风险。
8. DreamAPI/Sub2API可能覆盖请求质量；UI显示上游实际值。2026-09-13 当前图片工具上游持续返回 502，文本接口正常；这是仍未解除的外部阻断。
9. DreamAPI长Prompt采用语义压缩，不保证完整原文逐字发送到上游；面板会提示发生压缩。
10. 当前RunningHub账户实测并发上限是2；不要恢复为3。
11. RunningHub任务拿到任务号后禁止自动重提。
12. 旧运维/交接文档可能写“令牌鉴权、9个工作流、Ollama、旧deploy.py”等，均以本文和当前代码为准。
13. API 生图的生产出口依赖本机 `comfy_watchdog.py` 和 8198 SSH 反向隧道；本机离线时会明确失败，不会改走不确定的重复计费请求。
14. 本机 Codex 配置里的 DreamAPI 凭据不是官方 OpenAI API Key，可能随登录或供应商策略变化；失效时应更新远端环境文件，不能写入前端或仓库。
15. 任务账本暂未实施自动容量清理；在定义收藏感知、运行中任务保护和恢复窗口前不要直接按数量删除。
16. 供应商结果 URL 仍存在有前置条件的 SSRF 风险；严格白名单需要先收集 RunningHub 实际 CDN 域名和重定向合同。

---

## 16. 禁止事项

- 禁止提交或打印 `tools/creds.json`、`tools/id_ed25519`、任何`.env`、systemd EnvironmentFile。
- 禁止浏览器提交 workflow ID、节点ID、fieldName、模型文件或provider资产名。
- 禁止开放全部ComfyUI节点参数。
- 禁止拿附件中的成人默认Prompt做生产默认或复述到文档。
- 禁止把任务HTTP 200当作E2E成功；必须验证最终媒体。
- 禁止用单张最好图替代3-seed对照。
- 禁止在无证据时估算RH币；未返回就显示“未返回”。
- 禁止直接用旧 `tools/deploy.py` 发布当前生产版本。
- 禁止在工作树不干净、测试没跑、Agent审查未过时发布。
- 禁止为了让测试变绿恢复已经封堵的Prompt/ID/Key公开字段。

---

## 17. 推荐后续工作（非当前阻断）

按优先级：

1. 为公网入口加HTTPS反向代理，再设置Secure Cookie。
2. 引入稳定设备/用户身份，替代可重置匿名Cookie的会话限额。
3. 为style221/style222设计更有代表性的多题材3-seed挑战，产出合格预览图后再加入预览资产。
4. 为SCAIL2角色替换增加时序质量筛选或后处理，不掩盖单帧融合风险。
5. 将历史辅助脚本中的工作站绝对路径逐步迁移为CLI参数；生产构建器和当前测试主路径已可移植。
6. 对任务账本/本地生成媒体实施收藏感知的长期容量清理策略；不要误删运行中或收藏任务。
7. 若要重新启用认证，先设计从匿名签名会话到登录身份的平滑迁移，不要直接破坏现有历史所有权。
8. 采集 RunningHub 正常结果域名与重定向链，形成可回归的 CDN allowlist 后封堵条件性 SSRF。
9. 给 DreamAPI 本机出口增加独立存活探针和受控备用出口；不要把 `dreamapi.club` 的同机回环重新设为唯一生产路径。

---

## 18. 交接完成定义

下一个Agent只有在以下都成立时才能宣称新改动“完成”：

- 需求每一项都已实现；
- 相关RED测试先失败、修复后GREEN；
- 构建器重建成功；
- 内联JS和Python语法通过；
- 55+34双轨门或更新后的非空计数全部通过；
- Git diff无敏感值；
- 独立审查PASS；
- 正式preflight `ready=true`；
- 原子发布返回`PUBLIC_MARKERS_OK`与`REALISM_RELEASE_DEPLOY_OK`；
- 公网回读、systemd、watchdog和发布锁状态正常；
- 涉及供应商时完成真实单次POST、任务号、轮询和媒体机械验证；
- 对未验证或严格视觉失败的部分如实披露。

当前 `8c9a122` 已满足代码、测试、发布和页面回读条件；DreamAPI 图片媒体机械验证未通过，结论是供应商上游故障，不能列为成功验收项。
