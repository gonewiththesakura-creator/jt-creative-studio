# JT 远程 ComfyUI 控制面板 — 运维文档

> **早期历史资料：** 本文的“9 个工作流、旧鉴权和旧更新方式”等内容已经过时。当前运维只以 [`项目交接-2026-09-15.md`](项目交接-2026-09-15.md) 和 `tools/deploy_realism_release.py` 为准。

> 最后更新：2026-08-13（新增 ComfyUI 检测器 + 远程一键启动）

## 一、架构

```
手机/电脑浏览器
      │  http://8.210.125.65:8189  （阿里云安全组已放行）
      ▼
公网服务器 8.210.125.65（Ubuntu 24.04，无 GPU）
      │  systemd 服务 comfy-panel（纯 Python 标准库，零依赖）
      │  面板：固定参数提交 + 轮询结果 + 原图下载 + ComfyUI 检测/一键启动
      ▼
SSH 反向隧道（本机守护 comfy_watchdog.py 维护）
      ├─ 服务器 127.0.0.1:8199 → 本机 127.0.0.1:8188（ComfyUI 出图）
      └─ 服务器 127.0.0.1:8198 → 本机 127.0.0.1:8198（守护控制口，可远程启动 ComfyUI）
      ▼
本机 Windows ComfyUI（127.0.0.1:8188，RTX 3060 出图）
```

**要点：**
- 服务器不装任何大模型，只做"遥控器"；出图始终在本机 GPU。
- 面板只暴露 jt 目录全部 9 个工作流，且只允许改：提示词、长宽、批量、高清开关、种子、LoRA。不暴露 ComfyUI 原始 API。
- 访问需令牌（见 `PANEL_TOKEN.txt`）。
- **ComfyUI 检测器**：面板顶部实时显示 ComfyUI 在线/离线；离线时可用「⚡ 一键启动 ComfyUI」远程拉起本机 ComfyUI（通过 8198 控制口触发本机守护启动，通常 40–60 秒就绪）。

## 二、面板工作流（jt 目录全部 9 个）

| ID | 名称 | 速度 | 批量 | 尺寸 | 需Ollama |
|---|---|---|---|---|---|
| qwen2509 | Qwen 2509 · 身份换装换场景（生产基线） | 约2-3分钟/张 | 串行 | 可缩放 | 否 |
| qwen2511 | Qwen 2511 · 双参考低串扰 | 约1.5-3分钟/张 | 串行 | 可缩放 | 否 |
| flux4b | FLUX.2 Klein 4B · 快速草图 | 约30秒/张 | 原生批量 | 任意16倍数 | 否 |
| anima01 | Anima Base · 主生产（单LoRA 2号画风） | 约1分钟/张 | 串行 | 可缩放 | **是** |
| anima02 | Anima Base · 人物加画风（漫画剧情） | 约1-2分钟/张 | 串行 | 可缩放 | **是** |
| anima03 | Anima Aesthetic · 角色特写立绘 | 约1-2分钟/张 | 串行 | 可缩放 | **是** |
| anima04 | Anima Turbo · 快速背景分镜 | 约30秒/张 | 串行 | 可缩放 | **是** |
| qwen06corr | Qwen · 可选二次身份修正 | 约2-3分钟/张 | 1 | 跟随输入图 | **是** |
| anima07 | Anima · 同构图保脸轻修 d0.20 | 约1分钟/张 | 1 | 跟随输入图(768×1024) | **是** |

- 参考图：qwen2509 用 NEW02；qwen2511/flux4b 用 OLD007+OLD039；qwen06corr 图1成片+图2身份参考、anima07 输入图，均固定为 `input/char3_best_ref_NEW02.png`（在 ComfyUI input 目录替换同名文件即可换图）。
- 高清开关：0 关闭 / 1 ClearReality 极速2× / 2 UltraSharp 精细2×。
- 批量>1 时：qwen/anima 系逐张串行（每张独立种子），flux4b 原生一次出多张。
- **翻译后端（2026-08-13 第二次切换）**：改用 **DeepSeek API**（`https://api.deepseek.com/v1`，模型 `deepseek-chat`，比百炼便宜）。密钥在插件目录 `.env` 的 `DEEPSEEK_API_KEY`（key 不出本机）。优先级：节点字段 > 环境变量 DEEPSEEK/OPENAI/DASHSCOPE > 插件 .env 同名变量。
- **真实进度条**：面板通过 WebSocket（本地标准库实现）订阅 ComfyUI 的 `progress` 事件，同步 KSampler 真实采样进度（批量任务按 当前张采样进度/总张数 折算），前端进度条实时更新。已验证批量2张进度平滑递增。
- 大模型必须串行：面板全局锁，同一时间只跑一个任务。

## 三、本机需要运行的东西

```text
1. 面板守护      双击  D:\LAN-Share\lora\_work\comfy_panel\启动面板.bat
                 （无窗口后台运行：维护 SSH 隧道 8199+8198 + 控制口 8198）
                 这一项替代旧的「启动面板隧道.bat」；关闭窗口不影响守护运行
2. ComfyUI       守护会检测：未启动时面板可远程一键启动；
                 也可手动双击 D:\ComfyUI_Mie\1_1点击启动comfyui.bat
3. Ollama        已不再需要（中文翻译改走 DeepSeek）
```

- 守护脚本：`comfy_watchdog.py`（用内嵌 pythonw.exe 无窗口运行），职责 = 控制口 8198（/status、/start）+ SSH 隧道（8199→ComfyUI、8198→控制口，自动重连）+ 启动 ComfyUI。
- **密钥权限**：若重装系统或换机器，`tools/id_ed25519` 需修复权限系统 OpenSSH 才认：
  ```bat
  icacls "D:\LAN-Share\lora\_work\comfy_panel\tools\id_ed25519" /inheritance:r /grant:r "JT:F"
  ```

面板服务器端是 systemd 服务，开机自启、挂了自动重启，无需手动管。

## 四、服务器端管理（SSH 登入 admin@8.210.125.65）

```bash
systemctl status comfy-panel     # 状态
sudo systemctl restart comfy-panel   # 重启面板
journalctl -u comfy-panel -n 50  # 看日志
```

面板代码目录：`/home/admin/comfy-panel/`
- `server.py` 后端（改完 sudo systemctl restart comfy-panel）
- `templates/*.json` 工作流模板（占位符 {{PROMPT}} {{WIDTH}} {{HEIGHT}} {{BATCH}} {{HD}} {{SEED}} {{PREFIX}}）
- `config.json` 工作流元数据（新增工作流：加模板+配置项）
- `panel_data/jobs/` 历史出图存档

## 五、常见故障

| 现象 | 原因 | 处理 |
|---|---|---|
| 打开面板提示"comfy: ok=false" | ComfyUI 没起 / 隧道断了 | 面板顶部点「⚡ 一键启动 ComfyUI」；若提示守护未运行，先在本机双击「启动面板.bat」 |
| 面板显示「本机守护未运行」 | 本机没跑 comfy_watchdog.py | 在本机双击 `启动面板.bat` |
| 提交后一直"生成中"不动 | qwen 系每张 2-3 分钟，属正常；超过 15 分钟看 ComfyUI 队列 | 面板 `/api/health` 看 running_job |
| 服务器重启后面板访问不了 | systemd 已开机自启 | 无需操作；但**隧道在本机**，需重新双击 bat |
| 想换访问令牌 | 改 `/etc/systemd/system/comfy-panel.service` 里 PANEL_TOKEN | 改完 systemctl daemon-reload && restart；本地 PANEL_TOKEN.txt 同步改 |
| Anima 系列任务失败/提示翻译错误 | 本机 Ollama 没开 | 启动 Ollama（面板工作流卡片会显示 ⚠️需本机Ollama） |
| 预览图不显示/下载失败 | 旧版缓存了不带令牌的图片请求 | 强制刷新浏览器（Ctrl+F5）；新版已改用带令牌的 blob 加载 |

**部署注意**：`deploy.py` 只上传文件，**不会重启服务**（config 启动时加载）。每次改完 server.py/config.json 后必须：
```bash
sudo systemctl restart comfy-panel
```

## 六、安全提示（重要）

1. 面板有令牌但走明文 HTTP，且公网可达。令牌相当于唯一钥匙，别外传。
2. 服务器 SSH 密码过弱（文档里那串），已部署了 ed25519 密钥；强烈建议在阿里云控制台改掉密码或只留密钥登录。
3. tinyproxy（8888）是微信发布跳板，勿动；nginx 是 dreamapi 生产站点，勿动。
4. 本机 ComfyUI 只监听 127.0.0.1，公网无法直连 ComfyUI 原始接口，安全。

## 七、更新面板代码

本地改好 `server.py`/前端后，重新跑：

```bash
cd D:\LAN-Share\lora\_work\comfy_panel\tools
sshev\Scripts\python.exe deploy.py   # 全量重传 + 重启服务
```

（注意 deploy.py 会生成新令牌，如不想换令牌需改脚本复用旧值。）

## 八、证据与验证记录

- 本地 E2E：flux4b 批量2+ClearReality（1664×2496×2 张）；qwen2511 批量2+UltraSharp 1024×1536（精确）；qwen2509 768×1152（精确）；鉴权 401 正常；原图下载 Content-Disposition 正常。
- 隧道验证：服务器 curl 127.0.0.1:8199/system_stats 返回本机 ComfyUI（当前 0.33.0，2026-08-15 已从 0.19.3 升级）。
- ComfyUI 检测器 + 远程一键启动验证：杀掉本机 ComfyUI → 面板 POST /api/comfy/start → 51.3 秒后自动拉起，system_stats 正常。
- 公网验证：http://8.210.125.65:8189/api/health 见第五节前的记录。
