"""Use Browser Use to load /video, inject an already-done task card, call
pollTask, and verify that a <video> element is rendered with the MP4 URL."""
# This script is meant for browser_exec; kept here for documentation only.
