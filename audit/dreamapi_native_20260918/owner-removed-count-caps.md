# 用户确认删除网站人工次数限制

用户指出没有要求设置这些上限，并明确回复“删除这些次数限制”。因此删除网站 API／云端会话每小时 4 次、全站每小时 10 次及 24 小时 30 次的限制，不再用历史累计次数拒绝新生图。

保留原子任务登记、会话归属、同请求号去重、正在运行任务的并发保护、原任务恢复和上游状态不确定保护；中转站自身的权限、余额、内容审核和限流仍由上游决定。未删除或重置历史任务，没有修改上传限制。

模拟 HTTP 回归接受同一会话一小时内的 34 次 API／云端任务；去重后不新增任务。另验证 35 条并发持久化、会话隔离和 40 条历史失败记录不阻断新任务。测试不调用真实付费上游。

本次发布同时包含中文 API 错误分类。保留既有真实生图证据，但不声称进行了新的付费生成；固定此次运行内容摘要，并通过 AST 对比证明改动仅涉及错误展示、次数统计／任务登记和相应常量，生图请求及传输代码、路由、其他运行文件没有变化。发布检查已同步为用户确认的新规则，防止后续发布悄悄恢复旧上限。

此说明取代此前交付文档中的“保留全站总次数限制”表述。上线结果见 `error-localization-deployment.json`。
