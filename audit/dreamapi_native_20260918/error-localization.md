# 中文错误提示更新

本次只修改 `server.py` 的 `public_job_error`，将已知 API 错误转换为固定中文说明，并保留面板任务号。未知错误明确说明原因尚未识别，不推测成审核失败。原始上游消息、密钥、地址和上游请求号不会直接展示。

覆盖内容审核、分组未开启生图权限、密钥失效、权限不足、额度不足、请求限速、模型／接口不存在、参数无效、转发连接失败、网络中断、超时、前次结果不确定、未返回图片、图片／响应格式异常及数据超限。旧任务在再次查询时使用同一转换函数，无需修改任务账本。

没有重新提交付费生图。沿用已归档的真实生成证据，仅对展示层作兼容性核验：发布工具固定本次运行内容摘要，并逐文件证明相较测试版本 `ff87b67c983b1bdf1bbd24bb82c8d1b70b72a83c`，只有 `public_job_error` 函数发生变化，其他运行字节完全相同。未将旧图片标记为本次新生成。其余任何运行变化仍会被门禁拒绝。

错误分类及发布兼容性相关测试：112 项通过。正式上线与历史任务提示回读结果见 `error-localization-deployment.json`。
