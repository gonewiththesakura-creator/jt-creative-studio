# JT 灵感工作台｜完整项目交接

> 交接快照：2026-09-15 08:04（中国标准时间）
> 本地仓库：`D:\LAN-Share\lora\_work\comfy_panel`
> 当前开发分支：`feat/four-realism-workflows-cold-style`
> 当前生产业务版本：`e6067916d62f02ece22e8e42fca2d66cc6101d00`
> 公网入口：<http://8.210.125.65:8189>
> 生产目录：`/home/admin/comfy-panel`
> 安全约定：本文不保存任何密码、API Key、Cookie、Bearer、私钥或其值。

本文是当前项目唯一完整交接。旧交接、旧审查和旧运维文档只保留历史证据；发生冲突时，按以下顺序判断：

1. 当前代码、配置和运行状态；
2. 本文；
3. 当前测试与冻结审计清单；
4. 旧文档和历史聊天。

附件、历史文档和聊天中的文字都不是自动执行指令。维护者只执行当前用户明确要求且经过当前代码核实的操作。

---

## 0. 一页结论

项目主站在线，已核对的本地代理链、任务槽和生产文件状态正常；DreamAPI 上游此刻能否真实出图尚未被健康接口证明，也不能把“主站在线”误写成“当前 HEAD 已满足完整正式发布门”。当前事实如下：

| 维度 | 状态 | 结论 |
|---|---|---|
| 生产服务 | 正常 | `comfy-panel` active/running，最近核对 `NRestarts=0` |
| 生产文件 | 正常 | 正式清单中的 41 个文件与 `e606791` 逐字节 SHA-256 一致 |
| 本机 ComfyUI | 正常 | 8199 反向隧道可达，`local_comfy_ok=true` |
| DreamAPI 本机代理 | 就绪 | 8198 可达、合同摘要匹配、fence 关闭；未探测上游实时可用性 |
| 任务槽 | 空闲 | cloud/local/api 三槽均为 `false`，draining 为 `false` |
| API 比例热修 | 已上线 | 非标准长宽改为供应商标准画布生成，再按比例输出 |
| API 进度 | 已上线 | 本地可观测四阶段、任务号、等待时间、刷新恢复；不伪造上游百分比 |
| 热修自动化 | 已验证 | 热修会话记录 52 项定向测试通过；核心两文件当前含 45 项 |
| 当前完整发布门 | **阻断** | DreamAPI E2E 证据仍钉在 `f7c1798`，当前载荷是 `e606791` |
| 当前非方图付费 E2E | **缺失** | `e606791` 没有新付费生图；旧三模型统一证据均为 `1:1` |
| 安全 | 有高优先级待办 | HTTP 免登录、本机凭据 ACL 过宽、生产 systemd unit 权限不安全 |
| 灾备 | 不完整 | 没有 Git remote；`panel_data` 不受 Git 和代码发布事务保护 |
| 西雅图迁移 | 不建议 | 按用户提供的 1C/1GB 规格及 2026-09-14 延迟抽样，只适合实验出口、静态副本或冷备 |

最重要的边界：

- 当前生产服务健康，并且生产业务文件确实是 `e606791`。
- 当前 DreamAPI 三模型真实媒体证据属于 `f7c1798`，不能替 `e606791` 背书。
- `/api/health` 的 `dreamapi_contract_match=true` 目前不覆盖新增的“比例到供应商画布”映射，只证明旧 v2 请求能力集合一致。
- 下一次正式发布前必须解决证据漂移；禁止为了发布通过而只改哈希或伪造审计文件。

---

## 1. 新维护者的 15 分钟接手路径

### 1.1 先做只读核对

```powershell
cd D:\LAN-Share\lora\_work\comfy_panel

git status --short --branch
git log -5 --oneline
git rev-list --left-right --count main...HEAD
git remote -v

curl.exe -fsS --max-time 15 http://8.210.125.65:8189/api/health

python -c "import tools.deploy_realism_release as d; print(d.validate_dreamapi_migration_e2e_manifest())"
```

交接时最后一条的预期结果是：

```text
['DreamAPI runtime payload changed after tested release']
```

这是一道正确失败关闭的发布门，不代表线上已经故障。

### 1.2 必须先读的文件

1. `README.md`
2. 本文
3. `server.py`
4. `config.json`
5. `comfy_watchdog.py`
6. `build_unified_three_styles.py`
7. `build_realism_workbench.py`
8. `build_video_workbench.py`
9. `sources/video_business.js`
10. `tools/deploy_realism_release.py`

### 1.3 开始操作前的停止条件

出现以下任何情况，先停止重启、发布或付费验证：

- `/api/health` 任一 `cloud_busy/local_busy/api_busy` 为 `true`；
- `draining=true`；
- `dreamapi_uncertainty_fence=true`；
- 本地或远端出现 `.release.lock` 且事务状态没有确认；
- Git 工作树含不属于当前任务的改动；
- 无法确认将要修改的是构建源还是生成物；
- 需要付费 E2E，但用户没有明确授权本次付费提交。

---

## 2. 产品与用户路径

JT 灵感工作台是一个公网、免登录的 AI 图片/视频控制台。用户不需要接触 ComfyUI 节点、RunningHub workflow ID、LoRA 文件或供应商密钥。

### 2.1 主要页面

| 路由 | 页面 | 能力 |
|---|---|---|
| `/` | 创作台 | 10 种画风提示词随机器；RunningHub、本地 ComfyUI、DreamAPI 三种图片后端 |
| `/promptgen` | 创作台兼容入口 | 文件与首页字节一致，浏览器规范化到 `/` |
| `/realism` | 真人化/图片编辑 | 快速真人化、六套真人化流程、全能图片编辑，共 8 项 |
| `/video` | 视频工作台 | 视频生成、视频编辑、动作迁移，共 12 项 |

兼容入口：

- `/realcomic` 重定向到 `/realism?workflow=realcomic`；
- `/original-sketch`、`/original-graphic` 重定向到创作台相应画风；
- `realism_3in1` 已退休，只保留历史识别；
- “三步绘画”和“铅绘逐步上色”已取消，不要恢复。

### 2.2 当前结果交互

- 创作、真人化、视频都采用单一主媒体 + 横向缩略图 + 左右切换；
- 图片、视频和 ZIP 按真实类型展示，ZIP 只作为下载卡；
- 任务卡与结果画廊处于正常文档流，不再彼此覆盖；
- 真人化和视频桌面/移动布局已有 `overlap=0` 的审计证据；
- 历史与收藏按匿名签名会话隔离；
- 收藏保留可回放的业务选择，但不回显 provider 私有字段。

### 2.3 进度语义

普通液体按钮不显示数字百分比。DreamAPI 额外显示四个本地可观测阶段：

1. 面板创建任务并准备请求；
2. 请求已从面板发出，正在等待上游响应；
3. 解析结果或适配最终画布；
4. 文件保存完成。

页面显示任务号和已等待时间。任务初始 `progress_pct=0`，请求发出前写入 15，结果解析和画布适配分别写入 75、88，保存完成写入 100；前端把这些状态映射为 14%、48%、78%、100% 的四段轨道，其中 75/88 都属于第 3 阶段。这些只是 UI 里程碑，不是供应商实时进度，也不估算剩余时间。当前前端第 2 阶段仍显示“图像模型生成中”，但服务端在真正发起 HTTP 调用前就写入 `API_GENERATING` 和 `provider_started`，因此它**不能证明供应商已受理或已开始生成**；后续应把该标签改为“已提交请求，等待图像服务响应”。

---

## 3. 系统拓扑

```text
浏览器（公网 HTTP）
  |
  v
香港 Linux Relay 8.210.125.65:8189
  /home/admin/comfy-panel/server.py
  |-- 静态页面、任务状态、会话、历史、收藏、配额
  |-- RunningHub HTTPS
  |     |-- 创作台云端图片
  |     |-- 真人化 / 图片编辑
  |     `-- 视频工作流
  |-- 127.0.0.1:8199
  |     `-- SSH -R --> Windows 127.0.0.1:8188 / ComfyUI
  `-- 127.0.0.1:8198
        `-- SSH -R --> Windows 127.0.0.1:8198 / comfy_watchdog.py
                        |-- 启动与检查本地 ComfyUI
                        `-- 固定合同 HTTPS --> dreamapi.club/responses
```

### 3.1 Linux Relay 的职责

- 运行 `server.py` 和托管 `static/`；
- 保存可信工作流配置；
- 代理 RunningHub、DreamAPI 和本地 ComfyUI；
- 维护异步任务状态、幂等、配额、历史和收藏；
- 保存 DreamAPI 结果、本地预览和收藏媒体；
- 提供健康、release drain 和部署目标；原子发布与回滚由维护端的正式发布器通过 SSH 编排；
- 不运行 GPU 模型。

### 3.2 Windows 工作站的职责

- ComfyUI：`D:\ComfyUI_Mie\ComfyUI`；
- ComfyUI 监听：`127.0.0.1:8188`；
- 守护：`comfy_watchdog.py`；
- 日常入口：`启动面板.bat`；
- 双反向隧道：远端 8199 到本地 ComfyUI，远端 8198 到本地控制口；
- DreamAPI 请求使用独立可终止 worker，通过 watchdog 固定出口发送。

`启动面板隧道.bat` 是旧单隧道入口，不能替代日常 `启动面板.bat`。

### 3.3 运行依赖

- Linux 运行时：Python 3，后端主体使用标准库；图片解码、预览和裁切需要 Pillow；
- Windows：Python、PowerShell、OpenSSH、ComfyUI；
- 本机构建：Python 和 Node.js；
- 发布：Python、Paramiko、Pillow；
- 测试：pytest、Pillow，部分 UI 检查需要浏览器工具。

仓库没有 `pyproject.toml`、锁定 requirements 或 `package.json`。环境可复现性目前依赖现有机器，是明确技术债。

---

## 4. 仓库地图与权威边界

### 4.1 当前权威文件

| 路径 | 职责 |
|---|---|
| `server.py` | 单体后端、三图片后端、RunningHub 工作流、会话、任务、上传、收藏、限额、安全边界 |
| `config.json` | 21 个工作流的可信目录、业务参数、节点映射、provider ID |
| `comfy_watchdog.py` | Windows ComfyUI 守护、双隧道、DreamAPI 受限出口和不确定性栅栏 |
| `tools/start_comfy_watchdog.ps1` | 核对 watchdog 身份与合同后复用或无窗口重启 |
| `tools/panel_liveness_watchdog.py` | Linux 面板存活探测和受限自愈逻辑 |
| `deploy/comfy-panel-watchdog.service`、`deploy/comfy-panel-watchdog.timer` | Linux watchdog oneshot 单元与定时器 |
| `tools/build_preview_assets.py` | 从明确指定、已审查的源图生成工作流/画风 WebP、缩略图和 manifest |
| `sources/` | 创作台原始提示词池、风格 profile、视频业务逻辑 |
| `templates/` | 本地 ComfyUI API-format 模板；当前主路径使用 `anima02*.json` |
| `build_unified_three_styles.py` | 创作台权威构建器 |
| `build_realism_workbench.py` | 真人化权威构建器 |
| `build_video_workbench.py` | 视频页面构建器 |
| `sources/video_business.js` | 视频业务前端源 |
| `tools/deploy_realism_release.py` | 唯一正式发布器 |
| `tests/` | 脚本合同和 pytest 双轨测试 |
| `audit/` | 冻结 E2E、迁移和视觉证据 |

### 4.2 生成物

```text
sources/cold_promptgen_source.html
sources/sketch_promptgen_source.html
sources/graphic_promptgen_source.html
sources/hanmanga_profile.source.json
sources/retro_manga_luxury_profile.json
sources/style221_profile.json
sources/style222_profile.json
  -> build_unified_three_styles.py
     -> sources/pool_union_manifest.json
     -> static/index.html
     -> static/promptgen.html

经人工确认的外部预览源图
  -> tools/build_preview_assets.py --source-dir <目录> --retro-source <文件>
     -> static/previews/*.webp
     -> static/previews/*.thumb.webp
     -> static/previews/manifest.json

build_realism_workbench.py + static/previews/*
  -> static/realism.html

sources/video_business.js + build_video_workbench.py
  -> static/video.html

build_original_style_pages.py
  -> static/original_sketch.html
  -> static/original_graphic.html

build_realcomic_workbench.py
  -> static/realcomic.html
```

`tools/build_preview_assets.py` 的两个源参数都是必填项，且源文件名必须匹配脚本内映射。源图不在仓库内时，先确认来源、授权和文件身份；不要对未知目录盲目运行。

禁止只长期手改 `static/index.html`、`promptgen.html`、`realism.html` 或 `video.html`。下次构建会覆盖这些改动。

### 4.3 运行数据与非权威资产

- `panel_data/`：运行态数据，已被 Git 忽略；
- `evidence/`、`reports/`：本机历史输出，不是正式发布门；
- `video_wf/`：可再生成的供应商工作流归档，已忽略；
- `tools/*.remote.*`、`tools/jobs.json.remote.tmp`：本机诊断快照，已忽略，不是权威状态；
- 根目录 `*.bak-*`：历史备份，只用于追溯；
- `static/orig_sketch.html`、`static/orig_graphic.html`：旧页面遗留，不在正式发布清单；当前兼容页是 `static/original_*.html`；
- `audit/test_suite_report.json`：2026-09-06 的过期失败报告（41/42），不属于当前正式门；
- 旧 `build_templates*.py`、`add_*.py`、`update_*.py`：一次性迁移脚本，可能重写配置或模板，日常禁止执行；
- `build_notrans_templates.py`、`build_orig_pages.py`：会改写配置、模板或旧 `static/orig_*.html` 的历史脚本，不是当前构建入口；
- 多个 `tools/diag_*`、`verify_*`、`upload_*`：历史人工诊断脚本，不等于安全运维入口。

旧 `tools/deploy.py`、`deploy_ui_branch.py`、`deploy_nff_motion_release.py` 和 `deploy_liuli_realcomic_release.py` 已改为明确失败关闭。不要恢复它们。

### 4.4 Git 状态

- `main` 仍停在早期基线 `258266c`；当前开发分支承载全部后续成果；
- 本文落盘前，业务代码相对 `main` 领先 46 个提交；
- 仓库未配置 Git remote；
- 当前成果只有本机 Git 仓库和生产机副本，不能视为可靠灾备；
- 生产健康接口不返回 release SHA，版本身份目前依赖正式事务和逐字节哈希核对。

---

## 5. 构建与修改规则

前端修改后的标准重建：

```powershell
python build_unified_three_styles.py
python build_realism_workbench.py
python build_video_workbench.py
```

按修改范围补充：

```powershell
python build_original_style_pages.py
python build_realcomic_workbench.py
```

基础语法检查：

```powershell
node --check sources/video_business.js
python -m py_compile server.py comfy_watchdog.py build_unified_three_styles.py build_realism_workbench.py build_video_workbench.py
git diff --check
```

注意：构建命令会写生成物，不属于只读检查。先确认工作树、改动范围和生成关系，再执行一次；不要为“再确认一遍”反复重建。

`python server.py` 也不是无副作用的页面预览：它会创建/读取 `PANEL_DIR`、恢复任务、启动后台回填，并监听 `0.0.0.0:$PANEL_PORT`。只允许在隔离环境中使用临时数据目录、非生产端口和测试凭据运行；不要在工作站上对着生产数据目录随手启动第二实例。

---

## 6. 后端运行模型

`server.py` 是约 4,000 行的单体服务，使用有界 `ThreadingHTTPServer`：

- HTTP worker：32；
- backlog：128；
- 拒绝 worker：8；
- 大响应并发：4；
- 大请求并发：2；
- 普通 JSON 上限：42 MiB；
- 上传 JSON 上限：82 MiB；
- socket、请求体、响应体和大响应均有绝对时限。

公网普通 `_auth()` 当前按产品决定恒为 `True`。管理操作使用独立 `PANEL_RELEASE_TOKEN`：

- `/api/loras`；
- `/api/comfy/start`；
- `/api/admin/release-drain`，并额外限制 loopback 来源。

### 6.1 并发模型

- 创作台 cloud、local、api 各自 single-flight；
- local 与 cloud 使用不同资源，可以并行；
- 所有 RunningHub 活跃任务统一受账号总并发 2 约束；
- 视频任务可异步并发，但仍计入 RunningHub 总并发；
- `running` 和 `recovering` 都占用相应槽位；
- 发布前必须连续确认所有槽位空闲。

### 6.2 幂等原则

客户端提交 `client_request_id`。服务端以：

```text
签名会话 + client_request_id + effective_request_sha256
```

决定：

- 相同请求：返回已有任务；
- 相同请求号但内容不同：409；
- 不同签名会话：互不串任务。

浏览器刷新后恢复原任务号，不重新提交。RunningHub 获得 `taskId` 后必须先持久化再轮询；查询瞬时失败进入 `recovering`，只继续查询原任务。

---

## 7. 三种图片生成渠道

统一执行分发点是 `server.py:run_job()`。

### 7.1 RunningHub 云端图片

```text
POST /api/generate
  -> 服务端固定 style / LoRA / trigger
  -> 生成可信 nodeInfoList
  -> RunningHub workflow
  -> 立刻持久化 taskId
  -> 顺序查询原 taskId
  -> 保存远程 HTTPS 结果元数据
```

浏览器只能提交业务参数，不能覆盖 LoRA 文件、触发词、节点 ID、字段名或 provider workflow ID。

明确 BUSY/RATE 拒绝可按既定规则处理；一旦供应商可能已经接受请求，禁止自动更换请求号重提。结果 URL 目前保存为远程 URL，避免大文件同步下载阻塞 relay。

### 7.2 本地 ComfyUI

```text
POST /api/generate
  -> build_api() 注入可信模板参数
  -> relay 127.0.0.1:8199
  -> SSH reverse tunnel
  -> Windows ComfyUI /prompt
  -> WebSocket / history 读取真实采样进度
  -> 优先保存 WebP 预览
  -> 后台归档完整原图
```

预览成功后，后台原图归档失败不会把已经完成的生成任务改判失败。当前 ComfyUI 目录和模型布局写死在 Windows watchdog 中，迁工作站前必须参数化或完整复刻。

### 7.3 DreamAPI 图片

用户没有官方 OpenAI API Key。当前路径使用独立 DreamAPI Bearer，来源曾由旧 Codex 生图项目验证，但运行时只作为服务端秘密配置；浏览器和仓库都不应得到它。

```text
POST /api/generate（浏览器只提交比例和白名单选项）
  -> Linux 服务端选择最终尺寸和供应商标准画布
  -> gpt-5.6-luna + 单个 image_generation tool
  -> relay 127.0.0.1:8198
  -> Windows watchdog 严格校验请求合同
  -> 独立可终止 worker 单次 POST dreamapi.club/responses
  -> 返回 Base64 媒体
  -> Linux 解码、校验、cover/contain、保存 dreamapi.png
```

当前请求合同：

| 项目 | 值 |
|---|---|
| 方法与路径 | `POST https://dreamapi.club/responses` |
| 顶层模型 | `gpt-5.6-luna` |
| stream | `false` |
| tool 数量 | 1 |
| tool 类型 | `image_generation` |
| 顶层 `tool_choice` | 不存在 |
| `gpt-image-2` | 标准调度指令，不含 `action` |
| Flare | 标准调度指令，`action=generate` |
| Sunburst | 严格调度指令，`action=generate` |
| 自动重试 | 不允许不确定结果自动重试 |

不要改为 `/v1/responses` 或 `/v1/images/generations`。

模型与质量白名单：

- `gpt-image-2`：`low/medium/high/auto`；
- `gpt-image-2.5-flare`：`low/medium/high/xhigh/max/auto`；
- `gpt-image-2.5-sunburst`：`low/medium/high/xhigh/max/auto`。

UI 默认值：Flare、low、9:16、cover。API 固定每任务一张、不走本地高清链、Seed 不适用。

#### 比例与画布

| 用户比例 | 供应商画布 | 最终输出 | 处理 |
|---|---:|---:|---|
| `1:1` | `1024x1024` | `1024x1024` | 直接校正 |
| `2:3` | `1024x1536` | `1024x1536` | 直接校正 |
| `3:2` | `1536x1024` | `1536x1024` | 直接校正 |
| `9:16` | `1024x1536` | `864x1536` | cover 裁切或 contain 留白 |
| `16:9` | `1536x1024` | `1536x864` | cover 裁切或 contain 留白 |

浏览器不会向 API 后端提交任意 `width/height`。服务端会在上游 prompt 中同时说明供应商画布和最终安全裁切区。

#### 运行安全

- 上游硬超时：600 秒；
- 响应上限：96 MiB；
- 单图上限：32 MiB；
- 源图像素有上限；
- watchdog 只接受固定模型、质量、尺寸、指令和 action 组合；
- 上游 URL 固定，禁止任意目标和自动重定向；
- Windows Job Object 使用 `KILL_ON_JOB_CLOSE`；
- 请求发出前记录 `%LOCALAPPDATA%\JTComfyPanel\dreamapi-inflight.json`；
- 超时、断网或父进程退出会保留 uncertainty fence；
- fence 期间拒绝新付费请求和正式发布；
- fence 文件不是普通缓存，worker 活跃或结果未确认时不得删除。

长 prompt 会确定性压缩到上游可接受预算；这不保证原文逐字完整发送。UI 会标识发生过压缩。

---

## 8. 10 种创作画风

服务端可信映射在 `server.py:STYLE_PRESETS`。前端 profile 中的私有 LoRA 信息在序列化前删除。

| style_id | UI 短名 / 配置全名 | Trigger | LoRA | 默认强度 |
|---|---|---|---|---|
| `cold` | 冷脸萌 / 冷脸萌·低饱和细线稿 | `jt_style3_v2` | `05_style3_v2_step1600.safetensors` + `04_style3_step800.safetensors` | 0.7 / 0.6；另有 style-only 0.6 / 0 |
| `sketch` | 铅绘 / 铅绘·铅笔淡彩手绘风 | `jt_style1_v1` | `01_style1_step900.safetensors` | 0.7 / 0.6 |
| `original_sketch` | 原始铅绘 / 原始铅绘·原始选项池 | `jt_style1_v1` | `01_style1_step900.safetensors` | 0.7 / 0.6 |
| `graphic` | 古风 / 古风·东方平面插画风 | `jt_style2_v1` | `02_style2_step900.safetensors` | 0.7 / 0.6 |
| `original_graphic` | 原始古风 / 原始古风·原始选项池 | `jt_style2_v1` | `02_style2_step900.safetensors` | 0.7 / 0.6 |
| `hanmanga` | 古漫 / 古漫·东方淡彩漫画线稿 | `jt_liulistyle_v1` | `08_liuli_style_v1_step600.safetensors` | 0.7 / 0.6 |
| `nff` | NFF / NFF·半写实动漫画风 | `jt_nffstyle_v1` | `06_nff_style_v1_step2000.safetensors` | 0.7 / 0.6 |
| `retro_manga_luxury` | 复古日漫 / 复古日漫奢华时尚手绘 | `jt_style321_v1` | `09_style321_v1_step200.safetensors` | 0.4 / 0 |
| `style221` | 板绘 / 板绘·半写实厚涂 | `zxqelun` | `11_style221_v1_step400.safetensors` | 0.4 / 0 |
| `style222` | 铜版 / 铜版·雕刻排线厚涂 | `zxqavri` | `10_style222_v1_step400.safetensors` | 0.4 / 0 |

只有 style3 使用 v2。不要恢复错误映射 `jt_inkwash_v1`、`jt_softpaint_v1` 或 `jt_style2_v2`。

复古、style221、style222 的“随机全部”只随机关键类别，避免内容词压过画风核心。style221/style222 暂无达到质量标准的正式预览，UI 显示明确无预览状态；不要用低质量烟测图填充。

---

## 9. 21 个工作流完整目录

`config.json` 是唯一运行目录。下表中的 provider ID 只供服务端维护，禁止通过公网 `/api/workflows` 暴露或允许浏览器覆盖。

### 9.1 创作台

| 业务 ID | 名称 | 类型 | Provider ID |
|---|---|---|---:|
| `anima02` | Anima Base · 人物加画风（漫画剧情） | RunningHub + 本地 ComfyUI + DreamAPI | `2091826879766933505` |

### 9.2 真人化与图片编辑（8 项）

| 业务 ID | 名称 | 类型/输入摘要 | Provider ID |
|---|---|---|---:|
| `realcomic` | 快速真人化（原漫画转真人） | RunningHub AI App；图+需求 | `2025090022289973249` |
| `realism_krea2` | Krea2_动漫转真人 | 图+指令+尺寸+Seed | `2096139245572726785` |
| `realism_2511` | 动漫转写实真人2511（零偏移·高还原） | 图+指令+Seed | `2096138870382723074` |
| `realism_multisample` | 动漫转真人·多采超清天花板 | 图+批量+Seed | `2096103615374155777` |
| `realism_qwen_zi` | Qwen+ZI动漫转真人写实感洗图 | 图+正负指令+尺寸 | `2096101840168747010` |
| `realism_4k_text` | 超写实4K文生图 | 文本+作者设计 LoRA 开关+尺寸+批量 | `2095331229284470786` |
| `realism_zi_flowmatch` | 动漫转真人ZI洗图改（Z-Image+FlowMatch） | 图+指令+作者设计 LoRA 组开关 | `2096150319347859458` |
| `qwen_image_edit_zip` | 全能修改图生图 | 图+指令+批量+Seed；可返回 ZIP | `2097924547454918658` |

### 9.3 视频工作台（12 项）

| 分组 | 业务 ID | 名称 | Provider ID |
|---|---|---|---:|
| 视频生成 | `h3_t2v_i2v` | MiniMax H3 · 文生/图生视频+首尾帧（超级加速版） | `2093554209044480001` |
| 视频生成 | `h3_lightx2v` | MiniMax H3 · 8步加速图生视频（lightx2v） | `2093554154237509633` |
| 视频生成 | `h3_4step` | MiniMax H3 · 4步加速首尾帧视频 | `2093554183483215873` |
| 视频编辑 | `wardrobe` | Wan2.2 · 换装+迁移+服装带货（流量密码版） | `2093554049743339522` |
| 视频编辑 | `h3_edit_n` | MiniMax H3 · 双参考视频编辑 | `2098330246185926657` |
| 视频编辑 | `h3_edit_t3` | MiniMax H3 · 通用视频编辑 T3 | `2098330116945457154` |
| 视频编辑 | `h3_edit_remove` | MiniMax H3 · 移除人物/物品/元素 | `2097937340852727809` |
| 动作迁移 | `action` | Wan2.2 · 动作迁移（高仿动作） | `2093554104655167490` |
| 动作迁移 | `scail2_plus` | SCAIL2 Plus · 长视频人物替换/动作迁移 | `2096841102812053505` |
| 动作迁移 | `scail2_multi` | SCAIL2 · 动作迁移/人物替换（影视二创） | `2096840691372924929` |
| 动作迁移 | `wan_action_stabilized` | 动作迁移 · 抖动优化 | `2097924526817800193` |
| 动作迁移 | `scail2_action_replace` | SCAIL2 · 动作迁移/角色替换 | `2097870206308147201` |

### 9.4 8 个 editor 到实际运行流程的来源映射

| 业务 ID | 编辑页 ID | 实际运行 ID |
|---|---:|---:|
| `scail2_plus` | `2097130671957770242` | `2096841102812053505` |
| `scail2_multi` | `2097130786146328577` | `2096840691372924929` |
| `h3_edit_n` | `2098341535964643329` | `2098330246185926657` |
| `h3_edit_t3` | `2098341701604966401` | `2098330116945457154` |
| `h3_edit_remove` | `2098341695082573826` | `2097937340852727809` |
| `qwen_image_edit_zip` | `2098345504735592450` | `2097924547454918658` |
| `wan_action_stabilized` | `2098345513989357570` | `2097924526817800193` |
| `scail2_action_replace` | `2098345531529936898` | `2097870206308147201` |

这些映射来自来源文件、哈希、实时 schema 和生产只读验证的交叉证据，不能按附件顺序或名称猜测。

### 9.5 新增或修改 RunningHub 工作流

1. 获取 editor JSON 和 API JSON，并计算 SHA-256；
2. 用登录来源或可信只读 schema 确认 editor ID 到 run ID；
3. 在 `config.json` 只暴露作者真实业务输入；
4. 节点、模型、VAE、Sampler、Steps、CFG、设备和 LoRA 留在服务端；
5. 上传走 capability token，客户端不接触 provider `fileName`；
6. 优先复用通用上传/生成路由，不新增一套专用后端；
7. 更新 schema、目录、隐私、运行和发布门测试；
8. 只有需要证明外部合同或真实媒体时才做一次受控 E2E；
9. E2E 固定 `client_request_id`，取得任务号后只轮询；
10. 核对媒体类型、哈希、时长/尺寸、费用和历史唯一性。

---

## 10. HTTP 路由

### 10.1 页面与静态资源

- `GET /`
- `GET /promptgen`
- `GET /realism`
- `GET /video`
- `GET /realcomic`
- `GET /original-sketch`
- `GET /original-graphic`
- `GET /static/*`

文本资源支持 ETag 和 gzip；内容哈希预览使用长期 immutable 缓存。

### 10.2 健康与目录

- `GET /api/live`：进程存活和 worker 指标；
- `GET /api/health`：ComfyUI、DreamAPI 出口/合同/fence、drain、三任务槽；
- `GET /api/comfy/status`；
- `POST /api/comfy/start`：管理令牌；
- `GET /api/workflows`：脱敏目录并签发 session；
- `GET /api/loras`：管理令牌。

### 10.3 任务与媒体

- `POST /api/generate`：创作台三后端；
- `GET /api/job/{id}`：owner-only 单任务；
- `GET /api/jobs?scope=creator|realism|video[&style=...]`：owner-only 历史；
- `GET /api/image/{job}/{file}`；
- `GET /api/local-preview/{job}/{file}`；
- `GET /api/preview/...`；
- `GET /api/rh-preview/...`。

### 10.4 真人化与视频

- `POST /api/workflow-upload`；
- `POST /api/workflow-generate`；
- `POST /api/realcomic-upload`；
- `POST /api/ai-app-generate`；
- `POST /api/upload`；
- `POST /api/video-generate`。

### 10.5 收藏与发布

- `GET /api/favorites`；
- `POST /api/favorites`；
- `GET /api/favorite-image/{id}`；
- `GET /api/favorite-preview/{id}`；
- `POST /api/admin/release-drain`：loopback + 独立管理令牌。

---

## 11. 数据、会话与恢复

### 11.1 持久化布局

远端数据根：`/home/admin/comfy-panel/panel_data/`

| 路径 | 内容 |
|---|---|
| `jobs.json` | 完整任务账本、恢复和幂等依据 |
| `jobs/{job_id}/` | DreamAPI 图片、本地预览或本地归档 |
| `favorites.json` | 收藏元数据 |
| `favorites/` | 固化收藏媒体 |
| `upload_capabilities.json` | capability 哈希与 provider 资产映射 |
| `upload_usage.json` | 上传次数和字节用量 |

jobs/favorites 使用临时文件、fsync、原子替换和 `.bak`。主文件损坏时保留损坏副本并尝试上一版；没有有效备份时拒绝静默清空启动。

启动恢复规则：

- 有明确 RunningHub `taskId` 的 running 任务转 `recovering` 并继续查询；
- 无法确认提交结果的本地/API 任务标记失败，不自动再生成；
- 服务启动后恢复云任务并补取可确认的 RunningHub 币数。

### 11.2 匿名签名会话

Cookie 名为 `jt_session`：

- 随机 session ID + HMAC 签名；
- `HttpOnly`、`SameSite=Strict`；
- 一年 TTL，30 天后刷新；
- 当前 HTTP 下不能安全设置 `Secure`；
- 任务、历史、收藏、媒体和 capability 按 session hash 隔离。

删除 Cookie 会得到新匿名身份，所以它不是账户系统，也不能阻止用户重置会话额度。有效签名源依次是启动时存在的 `PANEL_SESSION_SECRET`、启动时存在的 `PANEL_TOKEN`，两者都没有时才是进程随机值；该进程随机值会在重启后丢失身份连续性。迁移时必须先确认并保留**实际有效签名源**，不能只假设某个变量存在。更换签名源会让旧 Cookie 无法继续访问原历史和收藏；换域名还会因 Cookie 作用域变化造成同样的用户侧断裂。

### 11.3 上传 capability

浏览器上传后只获得不透明 token。token 绑定：

- 签名会话；
- workflow；
- input slot；
- 媒体类型；
- 24 小时 TTL。

服务端持久层只存 token SHA-256，并校验 basename、扩展名、魔数/容器头和工作流输入类型。

### 11.4 配额

| 类型 | 单会话 | 全局 |
|---|---:|---:|
| 收费任务/小时 | 4 | 10 |
| 收费任务/日 | - | 30 |
| 上传次数/小时 | 20 | 100 |
| 上传字节/小时 | 500 MiB | 2 GiB |

收藏每个匿名 owner 最多 200 条。本地 ComfyUI 不计收费额度；幂等重复不重复计费。

### 11.5 公开数据最小化

公网任务输出不应包含：

- 正向/负向完整 prompt；
- provider task/response ID；
- Comfy prompt ID；
- 工作流运行 ID、编辑页 ID；
- 节点 ID、字段名、模型和 LoRA 文件；
- API Key、token、上传资产名；
- 原始供应商错误正文。

API 5xx 对外只显示上游状态码、可手动重试提示和面板任务号。内部原始错误只用于受控排障。

---

## 12. 测试体系与验证强度

### 12.1 双轨测试

仓库有两类 `test_*.py`：

1. 脚本合同：模块顶层执行，以进程退出码判定；
2. pytest：含 `test_*` 函数或 `Test*` 类。

正式发布器用 AST 分类。当前清单：

- 55 个脚本合同；
- 35 个 pytest 文件；
- 0 个允许失败例外；
- 测试清单摘要仍与审查基线一致。

完整非付费测试门：

```powershell
python -c "from tools.deploy_realism_release import classify_release_tests,run_release_tests; s,p=classify_release_tests(); print({'script_contracts':len(s),'pytest_files':len(p)}); run_release_tests(); print('FULL_RELEASE_TEST_GATE_OK')"
```

不要用普通 `pytest tests/` 代替，它会把脚本合同当作 collection 输入并可能被 `SystemExit` 中断。

### 12.2 当前 `e606791` 的验证边界

已完成：

- 比例到供应商画布映射测试；
- 9:16 供应商画布到最终 PNG 的裁切/保存测试；
- API 5xx 脱敏和可操作提示；
- 四阶段进度、任务号、等待时间；
- 失败、取消、recovering、刷新恢复；
- 1280x800 和 390x844 页面视觉检查，无横向溢出；
- 热修会话记录 52 项定向测试通过。

未完成：

- 当前 `e606791` 的正式发布门；
- 当前版本三模型付费 E2E；
- 当前版本 9:16 和 16:9 最终真实媒体证据；
- 比例映射进入服务端/工作站合同摘要。

### 12.3 第一性原理验证阶梯

不要重复做无信息增量的验证。按变更风险只执行一次足够的最小阶梯：

| 改动 | 最小验证 |
|---|---|
| 纯文档 | 链接/路径核对、敏感值扫描、`git diff --check` |
| 单页面样式 | 构建器 + 相关 UI 合同 + 一次桌面/移动截图 |
| 后端局部逻辑 | 相关 RED/GREEN 测试 + 语法检查 |
| 共享合同/发布文件 | 相关测试 + 完整 55/35 门一次 |
| 供应商请求合同 | 非付费测试后，每模型一次顺序真实媒体 E2E |
| 正式生产发布 | 一次完整发布事务 + 一次发布后健康/字节核对 |

真实付费 E2E 只用于证明无法由本地测试证明的供应商合同，不用于反复“看看还行不行”。

---

## 13. DreamAPI 迁移、故障与热修时间线

### 13.1 已证实的迁移

旧任务“生成女孩写真图”并未使用官方 OpenAI API。复查其真实执行过程后，本项目从错误的 `gpt-5.6-sol`、统一 action 和丢失逐模型指令差异，迁回当前 Luna + 逐模型合同。

`f7c179871747355d11cb5f2789f561a256f671d5` 同一生产候选上的三模型证据：

| 模型 | 面板任务号 | 指令/action | 结果 |
|---|---|---|---|
| `gpt-image-2` | `36aa8e4c8776` | 标准；省略 action | 最终 PNG |
| Flare | `4637a3f0b1f0` | 标准；generate | 最终 PNG |
| Sunburst | `622a20c8e528` | 严格；generate | 最终 PNG |

三项都是单次 POST、顺序轮询和最终媒体回读，无自动重试；三项比例都是 `1:1`。冻结证据位于 `audit/dreamapi_migration_20260914/`。

### 13.2 任务 `8db1b0bcf0ab`

经生产任务记录诊断：

- 模型：Flare；
- 请求质量：low；
- 比例：9:16；
- 适配：cover；
- 2026-09-14 23:23:01 左右创建；
- 面板在发起 HTTP 请求前记录了 `provider_started`；现有数据不能证明供应商已受理或立即开始生成；
- 约 54.9 秒后收到明确 DreamAPI HTTP 502；
- 没有 response ID、图片或文件；
- 本机 uncertainty fence 已正常清除；
- 服务、隧道和合同健康当时正常。

可以确定的是“这次请求收到了上游 502”。不能确定供应商内部根因，也不能仅凭这一次记录断言 9:16 自定义尺寸是唯一原因。旧项目对标准方图也出现过同类间歇 502。

账单状态不能从现有记录确认。因为已收到明确终态而非超时，页面允许用户自行手动重试，但不会自动 POST，也提示不要连续提交。

### 13.3 `e606791` 热修

热修完成了三件事：

1. 9:16/16:9 不再直接作为供应商工具尺寸；先用受支持的 2:3/3:2 画布生成，再输出目标比例；
2. 上游 5xx 对外显示状态码、任务号和手动重试提示，不泄露内部正文；
3. API 增加本地可观测四阶段、任务号、已等待时间和恢复状态；当前第 2 阶段文案仍会高估可证明范围，见第 2.3 节。

该热修已经部署，相关事务记录为 committed/completed，生产 41 个正式文件与 `e606791` 字节一致。它没有走发布器 `main()` 的完整门；热修后也没有再触发付费生图。

### 13.4 当前证据漂移

发布器仍固定：

```text
DREAMAPI_TESTED_RELEASE_COMMIT=f7c179871747355d11cb5f2789f561a256f671d5
recorded runtime payload=631f0a5cd36b648165956fd817d79fa66ba83934746da0bd7ed7c207cbec60f0
current e606791 payload=7881885a6a47d5cd8185a29487ea18bcae89b02a3d19d8031f0b84b41622c8cd
```

所以 `validate_dreamapi_migration_e2e_manifest()` 正确返回：

```text
DreamAPI runtime payload changed after tested release
```

禁止直接把常量和 manifest 改成新 SHA。正确恢复路线：

1. 先让 DreamAPI 合同摘要显式覆盖“用户比例 -> provider 画布”的映射，并让 watchdog 白名单只表达真实 provider 画布；
2. 完成相关 RED/GREEN 和完整非付费门；
3. 设计一次可审计的 bootstrap 发布，不用历史密码脚本、不跳过 idle/drain/rollback；
4. 在同一生产字节上，经用户明确授权，顺序完成三个模型各一次真实媒体 E2E，并覆盖 1:1、9:16、16:9；
5. 校验最终 PNG、实际模型/质量/尺寸、无重复提交和生产文件身份；
6. 生成脱敏证据，更新 commit pin、payload digest 和 manifest；
7. 再运行完整发布门，确认标准发布路径恢复。

这是下一轮最重要的工程任务，但不是当前线上可用性的阻断。

---

## 14. 正式发布与回滚

### 14.1 唯一正式入口

当前 drain 合同已经安装。解决第 13.4 节证据漂移后，标准正式发布流程才是：

```powershell
cd D:\LAN-Share\lora\_work\comfy_panel

git status --short --branch

python tools/deploy_realism_release.py --allow-unauthenticated-public

python tools/deploy_realism_release.py --execute --allow-unauthenticated-public
```

不带 `--execute` 的命令只做本地预检，返回非零是设计行为；它不会运行完整测试。带 `--execute` 的正式入口会在远端变更前自行运行完整 55/35 测试门，成功事务只需跑这一次。若目的只是单独诊断测试，可改为运行第 12.1 节的 `run_release_tests()` 命令，但不要随后机械地再执行正式发布并把同一完整门重复两遍。当前证据漂移存在时，即使传 `--execute` 也应失败，不要绕过。

`--bootstrap-drain-contract` 只用于早期旧服务迁移；当前生产已安装 drain 合同，禁止再次使用。

### 14.2 发布器保证

正式发布器会：

1. 要求 Git 工作树干净并锁定同一 HEAD；
2. 验证 E2E 清单和双轨测试；
3. 从审核过的 Git commit 读取不可变 payload；
4. 固定 SSH 主机指纹并使用 Ed25519；
5. 获取 `.release.lock` 并创建 UUID 事务；
6. stage、manifest SHA-256、权限和 fsync；
7. 连续确认公网任务槽空闲；
8. 通过 loopback 管理接口开启 admission drain；
9. 隔离 watchdog 并在必要时保持“启动即排空”；
10. 原子切换整组文件并逐字节回读；
11. 启动新服务并确认它仍处于 drain；
12. 在 drain 状态核对公网 live/health、大响应、图片、工作流、合同和 marker，并安装/验证 watchdog；
13. 所有发布验证通过后写入 `committed`；
14. commit 后才清理启动 drain、当前 drain、事务目录和发布锁，重新开放任务。

失败处理分三类：切换前失败不需要代码回滚；切换后但 commit 前失败会尝试回滚，回滚状态不确定时停服并保留锁；commit 后的清理失败不会回滚已确认版本，但会保留锁并要求人工核对，服务不一定停止。

成功必须同时看到：

```text
PUBLIC_MARKERS_OK
REALISM_RELEASE_DEPLOY_OK
```

### 14.3 回滚边界

- 代码事务只覆盖正式清单中的 41 个生产文件；
- `panel_data` 不属于代码发布事务；
- 代码回滚不会恢复被删除或损坏的任务、收藏和媒体；
- pre-commit 回滚不确定时会停服并保留锁，禁止手工删锁强发；
- post-commit 清理失败时已确认版本可以仍在运行，但锁会保留，必须先审计 phase、服务、drain 和文件身份；
- 生产存在一个 2026-09-14 的历史未完成事务目录，无当前锁、不影响服务；清理前仍需人工确认内容和事务身份。

---

## 15. 日常运维与排障

### 15.1 公网健康

```powershell
curl.exe -fsS --max-time 15 http://8.210.125.65:8189/api/live
curl.exe -fsS --max-time 15 http://8.210.125.65:8189/api/health
```

不要只看顶层 `ok`。必须核对 Comfy、DreamAPI egress、contract、fence、drain 和三个 busy 字段。

### 15.2 Windows

```powershell
Invoke-RestMethod http://127.0.0.1:8198/status | ConvertTo-Json
.\启动面板.bat
```

启动器会核对现有 watchdog 进程的完整命令行和合同 SHA，再决定复用或重启。

### 15.3 远端

```powershell
ssh -i tools/id_ed25519 -o BatchMode=yes -o StrictHostKeyChecking=yes admin@8.210.125.65
```

登录后：

```bash
systemctl status comfy-panel --no-pager
systemctl status comfy-panel-watchdog.timer --no-pager
systemctl is-enabled comfy-panel comfy-panel-watchdog.timer
systemctl show comfy-panel -p FragmentPath -p DropInPaths -p User -p WorkingDirectory -p ExecStart -p EnvironmentFiles
sudo journalctl -u comfy-panel -n 200 --no-pager
sudo journalctl -u comfy-panel-watchdog.service -n 100 --no-pager
test ! -e /home/admin/comfy-panel/.release.lock || stat /home/admin/comfy-panel/.release.lock
find /home/admin/comfy-panel/.release-transactions -mindepth 1 -maxdepth 1 -type d -printf '%TY-%Tm-%Td %TH:%TM %f\n' 2>/dev/null | sort | tail -5
curl -fsS http://127.0.0.1:8198/status
curl -fsS http://127.0.0.1:8199/system_stats
```

watchdog service 是 oneshot/static 单元，空闲时 `inactive/static` 正常；timer 应为 active/enabled。

### 15.4 故障定位顺序

1. `/api/live` 失败：面板进程、端口或防火墙；
2. `/api/live` 正常但 `/api/health` 异常：逐字段定位；
3. 8198 失败：Windows watchdog 或 SSH 隧道；
4. 8199 失败但 8198 正常：本地 ComfyUI/8188；
5. `contract_match=false`：服务端与工作站 watchdog 请求合同漂移；
6. `uncertainty_fence=true`：检查 paid worker 并等待安全窗口，禁止重提；
7. 任一 busy=true：等待原任务，不重启、不发布；
8. RunningHub 有 taskId：只查原任务；
9. 用户提供面板任务号：只读取该记录所需字段，不导出整份账本。

任务排障建议查看：

- `id/status/provider_status/generation_backend`；
- `created/submit_started/provider_started/provider_finished/elapsed`；
- `client_request_id` 是否去重；
- DreamAPI 的 model/quality/ratio/fit/provider size/actual size；
- 是否存在 provider task/response ID 和最终 images；
- 内部错误类型；
- uncertainty fence 和隧道状态。

不要在聊天、工单或公开日志粘贴 prompt、Bearer、provider 私有 ID 或完整 `jobs.json`。

### 15.5 当前可观测性缺口

- `server.py` 默认关闭 HTTP access log；
- Windows watchdog 和隧道没有持久日志；
- 健康接口没有 release SHA；
- 外部付费任务的账单状态不总能由任务记录判断；
- 主要业务错误依赖 `panel_data/jobs.json` 和 systemd journal。

后续应增加脱敏结构化事件日志、release SHA、provider request correlation 和有限留存，而不是打印原始响应。

---

## 16. 凭据与安全边界

### 16.1 只记录位置，不记录值

本机 Git 忽略：

- `tools/id_ed25519`
- `tools/id_ed25519.pub`
- `tools/creds.json`
- `PANEL_TOKEN.txt`
- `.env` / `.env.*`
- `panel_data/`

远端环境：

- `/home/admin/comfy-panel/panel.env`，当前应为 `0600 admin:admin`；
- `/etc/comfy-panel/release.env`，当前应为 `0600 root:root`；
- `/etc/comfy-panel.d/dreamapi.env` 是可选路径，交接时不存在；
- systemd drop-in：`20-dreamapi.conf`、`90-release-token.conf`、`credentials.conf`。

主要运行变量：

- `COMFY_URL`
- `CONTROL_URL`
- `PANEL_PORT`
- `PANEL_DIR`
- `PANEL_TOKEN`
- `PANEL_SESSION_SECRET`
- `RUNNINGHUB_API_KEY`
- `DREAMAPI_KEY`
- `DREAMAPI_BASE_URL`
- `DREAMAPI_EGRESS_URL`
- `PANEL_RELEASE_TOKEN`

`server.py` 顶部旧注释仍写 `PANEL_TOKEN required (auth)`，但当前公网 `_auth()` 已取消。实际用途主要是未显式提供 `PANEL_SESSION_SECRET` 时派生稳定会话签名；不要被旧注释误导。

### 16.2 已确认的高优先级问题

1. **本机凭据 ACL 过宽。** `tools/creds.json` 和 `PANEL_TOKEN.txt` 当前允许普通本机用户读取，Authenticated Users 还可修改。目标状态应只允许当前维护账号、Administrators 和 SYSTEM；修改前先记录 ACL 并确保不会锁掉当前账号。
2. **生产 systemd unit 可被服务账号修改。** `/etc/systemd/system/comfy-panel.service` 当前为 `admin:admin 0664`，而 systemd 以 root 加载。不能直接把未经审查的现有内容“扶正”为 root 文件。先只读核对 `FragmentPath/DropInPaths/ExecStart/User/WorkingDirectory/EnvironmentFiles`、记录 unit 哈希，并确认没有异常 root 指令；不要输出 EnvironmentFile 的值。确认后在维护窗口改为：

   ```bash
   sudo chown root:root /etc/systemd/system/comfy-panel.service
   sudo chmod 0644 /etc/systemd/system/comfy-panel.service
   sudo systemctl daemon-reload
   ```

   修改后核对 unit 内容、服务 active 状态和健康，不需要为了验证反复重启。

3. **西雅图实例密码曾在聊天中暴露。** 必须通过服务商控制台轮换；新密码不得写入本文、仓库或脚本。

### 16.3 其他安全风险

- 公网是 HTTP 且免登录；签名 Cookie 不能防链路窃听；
- 删除 Cookie 可重置匿名身份；
- Windows SSH 隧道使用 `StrictHostKeyChecking=accept-new`，新机器首次连接前应人工核对指纹；
- 多个历史诊断脚本仍使用密码登录和 `AutoAddPolicy`，不要作为日常工具；
- `tools/plink.exe` 是仓库内历史二进制，应确认来源后决定保留；
- RunningHub 结果 URL 仍有条件性 SSRF 风险，需要真实 CDN/重定向 allowlist；
- 无依赖锁，升级环境可能造成不可复现变化；
- 无 Git remote；仓库和正式发布流程中未发现 `panel_data` 自动备份，机器损坏会造成不可恢复风险。

---

## 17. 当前生产快照

以下是 2026-09-15 08:04 CST 的只读快照，不是永久保证：

```json
{
  "ok": true,
  "comfy": true,
  "local_comfy_ok": true,
  "dreamapi_configured": true,
  "dreamapi_workstation_egress": true,
  "dreamapi_contract_match": true,
  "dreamapi_contract_sha256": "665d283bd420f76f031d9530f1d757f022d6cc16a5c9e9bc315e4966da797959",
  "dreamapi_uncertainty_fence": false,
  "draining": false,
  "cloud_busy": false,
  "local_busy": false,
  "api_busy": false
}
```

同一轮只读核对：

- 41 个正式发布文件与本地 `e606791` SHA-256 全匹配；
- `comfy-panel` active/running，`NRestarts=0`；
- watchdog timer active/enabled；
- Windows watchdog 与 SSH 隧道持续运行；
- 远端 8198/8199 均返回 200；
- 无 `.release.lock`、启动 drain drop-in 或 bootstrap nft/Requires 栅栏；
- 主机磁盘约 49 GiB，已用约 19 GiB；
- `panel_data` 约 1.8 GiB；
- 内存约 3.4 GiB，可用约 1.7 GiB；
- 无 swap；
- 从本轮面板启动时间起及当日 watchdog 查询窗口内，warning 级 systemd 日志计数为 0。

重要限定：`dreamapi_workstation_egress=true` 只证明 8198 本机代理可达、合同摘要匹配且 fence 关闭；`dreamapi_configured=true` 只证明 key 非空。健康接口没有调用 DreamAPI 上游，不能证明凭据仍有效或上游此刻可出图。当前合同 SHA 还是旧 v2 能力摘要，新增 `DREAMAPI_PROVIDER_SIZE_BY_RATIO` 没有进入摘要；比例热修的当前生产身份由 41 文件逐字节核对证明。

---

## 18. 审计证据与版本历史

### 18.1 当前使用的证据

- `audit/private_realism_workflows/e2e_manifest.json`
- `audit/scail2_video_e2e.json`
- `audit/retro_manga_panel_e2e_cloud_w04.json`
- `audit/dreamapi_creator_live_e2e.json`
- `audit/dreamapi_sidebar_long_prompt_e2e.json`
- `audit/dreamapi_migration_20260914/verification.json`
- `audit/style221_222_panel_e2e_20260913.json`
- `audit/uploaded_workflows_live_schema_20260912.json`
- `audit/uploaded_workflows_live_e2e_20260912.json`
- `audit/frontend_workbench_review/verification.json`
- `tests/fixtures/uploaded_workflow_provenance.json`

这些是冻结声明、媒体哈希和本地/生产一致性证据，不是供应商数字签名。任务自报不能证明运行 Git SHA。

### 18.2 关键版本线

| 日期 | 提交 | 内容 |
|---|---|---|
| 2026-08-31 | `258266c` | 重设计前基线，当前 `main` |
| 2026-09-06 | `7216472` | 接入私有真人化工作流 |
| 2026-09-08 | `5bf2001` | 接入 SCAIL2 视频工作流 |
| 2026-09-11 | `bf74768` / `ecf44fe` | 复古画风、面板加固和真实云证据 |
| 2026-09-12 | `ec82fa1` | DreamAPI 与精选 RunningHub 工作流 |
| 2026-09-12 | `1df8e50` / `9d644f9` | 发布失败关闭和恢复耐久性 |
| 2026-09-13 | `67f696f` | 公网任务数据最小化 |
| 2026-09-13 | `8c9a122` | 面板安全加固和本机图片 API 出口 |
| 2026-09-14 | `bd14286` / `9ddda8b` | 迁移旧项目真实 DreamAPI 调用合同 |
| 2026-09-14 | `f7c1798` | 三模型同版本候选和生产 E2E 基线 |
| 2026-09-14 | `5277d89` | 冻结迁移证据 |
| 2026-09-15 | `e606791` | API 比例、5xx 提示和四阶段进度热修 |

---

## 19. 已知产品与质量边界

- DreamAPI 是第三方中转，不是官方 OpenAI API；供应商仍可能间歇 5xx；
- 请求质量是偏好，上游可能覆盖，结果卡显示实际值；
- 长 prompt 可能语义压缩；
- 9:16/16:9 热修有本地合同测试，当前版本尚无真实付费媒体证据；
- API 第 2 阶段当前显示“图像模型生成中”，实际只能证明本地 HTTP 请求已开始，不能证明供应商受理；
- style221/style222 属于相近画风家族，跨题材区分度有限，无合格正式预览；
- `h3_edit_remove` 实测主结果有手部模糊风险；
- `scail2_action_replace` 有已知单帧双脸/躯干融合风险；
- `wan_action_stabilized` 已通过 4 秒、24 帧严格挑战者审查；
- T3/移除的第二输出是作者设计的原片/生成片对比；
- SCAIL2 第一输出才是主视频，第二输出是输入/参考/结果对比；
- RunningHub 实测账号并发为 2，不要提高到 3；
- 没有 provider 返回的币数时显示“未返回”，禁止估算；
- 任务账本没有收藏感知的自动清理策略；
- 生产数据已有约 1.8 GiB，容量和备份问题已从长期风险变成实际运维事项。

---

## 20. 服务器迁移结论

候选是美国西雅图 `US-SEA-BGP-01`。1C/1GB 和运行 sing-box 是用户提供信息，本项目没有完成主机内验证；2026-09-14 的外部检查只确认 SSH 端口可达，所提供密码未能登录，当前需要公钥。因此 CPU、磁盘、swap、防火墙和 sing-box 实际状态均未知。

结论：**保留香港为主站，不整体迁移到西雅图。**

理由：

- 2026-09-14 一次性网络抽样中，工作站到香港约 28-36 ms，西雅图约 162-328 ms；这不是长期 SLA；
- 1C/1GB 同时承担代理、最高 96 MiB API 响应、任务账本和媒体代理，OOM/尾延迟风险高；
- Windows GPU 和 8198/8199 隧道仍在国内；
- 2026-09-14 的网络抽样显示 DreamAPI 路径更接近香港；整体迁移可能增加大响应跨洋传输，仍需切换前复测；
- `panel_data`、Cookie 签名和收藏媒体都是有状态数据，不能只复制代码；
- 当前没有稳定域名/HTTPS，换 IP 会破坏浏览器身份连续性。

西雅图可选用途：

- 隔离的 DreamAPI 出口实验；
- 无状态静态副本；
- 加密冷备；
- 不承诺主站切换的网络探针。

未来若迁完整主站：

1. 至少 2 GiB，建议 4 GiB 内存；
2. 分别实测用户访问、RunningHub、DreamAPI 和工作站隧道；
3. 先上线稳定 HTTPS 域名和 Secure Cookie；
4. 确认并保持第 11.2 节所述的实际有效会话签名源；
5. 在源站开启 drain，反复确认所有任务槽已空闲，再停服务完成最终同步；若不停服务，必须改用经过验证的应用/文件系统一致性快照；
6. 演练 8198/8199 切换和回滚；
7. 旧站保留只读回退窗口；
8. 切换后核对最终媒体，不只看 HTTP 200。

优先候选地域仍是香港；其他地域只能按实测比较东京/大阪、新加坡、首尔，不能按标签猜延迟。

---

## 21. 灾备与数据连续性

当前最薄弱的不是代码回滚，而是“没有独立代码远端 + 没有明确数据备份”。

### 21.1 必须保护的资产

1. Git 仓库和当前分支历史；
2. `panel_data/` 中的任务、prompt、上传映射、收藏、图片和视频；这是用户数据，不能当普通构建缓存；
3. Windows ComfyUI 的模型、custom nodes、Python/依赖版本和必要配置，或一套经过恢复验证的可重建清单；
4. 经安全审查后重新建立基线的主 systemd unit、drop-ins、watchdog 单元和部署参数；当前可写的 unit 不能未经审查直接当作可信备份；
5. 环境秘密、实际有效会话签名源、SSH 私钥与固定主机指纹材料，必须加密、最小授权并与代码分开保存；
6. RunningHub、DreamAPI 和服务器服务商账号的恢复方式、2FA 与责任人信息，不能只依赖当前浏览器会话。

### 21.2 建议恢复顺序

1. 新主机安装受控 Python/Pillow 环境；
2. 从可信 Git remote 或离线镜像恢复代码；
3. 恢复经过审核的 systemd/watchdog 配置和受控运行环境；
4. 以原权限恢复环境文件、SSH 身份和主机指纹 pin，不把秘密放进 Git；
5. 恢复 Windows ComfyUI 环境并核对模型/custom nodes 身份；
6. 恢复 `panel_data`，核对 JSON、媒体数量、清单哈希和抽样可读性；
7. 恢复并核对原来的实际有效会话签名源；
8. 建立 8198/8199 隧道并只在 loopback 验证；
9. 启动 drain 状态服务；
10. 核对 health、任务恢复、收藏和最终媒体；
11. 最后开放公网并观察；
12. 保留旧实例回滚窗口。

一致性备份的安全顺序是：开启 drain，反复确认 cloud/local/api 三槽都空闲，再停止面板服务并做最终同步/快照；完成后先以 drain 状态启动。drain 只拒绝新的生成任务，不阻止收藏等全部写入，也不会替你等待既有任务结束，因此“看到 idle”或“只开 drain”任一项单独都不够。若业务要求不停服，必须使用经过验证的应用级或文件系统一致性快照方案。

`panel_data` 备份必须异机加密、最小授权，定义保留与删除周期，并保存清单/哈希；至少做一次从空目录恢复的演练，验证 JSON、收藏和最终媒体可读。正式接手后还要按可接受的数据丢失量和恢复时间定义 RPO/RTO，不能只确认“有一个压缩包”。

---

## 22. 禁止事项

- 禁止提交、打印或聊天粘贴密钥、密码、Cookie、Bearer、私钥和 EnvironmentFile；
- 禁止使用聊天中已暴露的西雅图密码；
- 禁止浏览器提交 provider workflow ID、节点 ID、fieldName、模型文件或 LoRA 资产名；
- 禁止开放全部 ComfyUI 节点参数；
- 禁止只改 `static/*.html` 而不改构建源；
- 禁止执行旧 deploy 入口或历史密码/AutoAddPolicy 脚本发布；
- 禁止把任务 HTTP 200 当作 E2E 成功；必须核对最终媒体；
- 禁止获得 taskId 后自动重新 POST；
- 禁止在 uncertainty fence 内重提；
- 禁止在 busy、draining、工作树不干净或锁状态不明时发布/重启；
- 禁止为使发布门变绿直接改 commit pin、digest 或伪造 manifest；
- 禁止无收藏感知地按数量删除任务或媒体；
- 禁止手工删除不确定回滚锁后强发；
- 禁止把 `audit/test_suite_report.json` 当当前测试结果；
- 禁止用单张最好图替代受控 seed/模型对照；
- 禁止在无证据时估算 RunningHub 币、DreamAPI 账单或供应商根因；
- 禁止把西雅图端口可达写成“迁移可行”；
- 禁止无信息增量地反复跑同一验证，尤其禁止重复付费验证。

---

## 23. 待办优先级

### P1：立即安排

1. 轮换聊天中暴露的西雅图实例密码；
2. 收紧本机 `tools/creds.json`、`PANEL_TOKEN.txt` ACL；
3. 审查生产 `comfy-panel.service` 的有效内容与哈希，确认安全后改为 `root:root 0644`；
4. 配置可信私有 Git remote 或加密离线镜像；
5. 建立 `panel_data` 一致性快照、保留周期和恢复演练；
6. 修复 DreamAPI 比例映射合同摘要，并按第 13.4 节恢复正式发布证据。

### P2：近期

1. HTTPS、稳定域名、Secure Cookie 和稳定身份；
2. 健康接口增加 release SHA；
3. Windows watchdog/隧道脱敏持久日志；
4. 任务账本收藏感知清理和磁盘水位报警；
5. RunningHub CDN/重定向 allowlist，降低 SSRF 风险；
6. 建立 Python/Node 依赖清单和锁定版本；
7. 把历史 SSH 脚本迁移到统一的固定主机指纹安全库；
8. 清理被忽略的远端快照和历史事务，但先确认不属于恢复证据；
9. 将 API 第 2 阶段文案改为“已提交请求，等待图像服务响应”，并增加合同测试，避免暗示供应商已经受理。

### P3：质量改进

1. style221/style222 多题材挑战和合格预览；
2. SCAIL2 角色替换时序筛选或后处理；
3. H3 移除结果的手部质量筛选；
4. 将 `server.py` 按路由、provider、持久化和安全边界逐步拆分；
5. 将硬编码 Windows 路径参数化；
6. 按确认无引用的原则归档根目录一次性迁移脚本和旧备份。

---

## 24. 交接完成定义

本文层面的“全部交接”已经覆盖：

- 产品入口与兼容路由；
- 10 种画风和 21 个工作流；
- 三种图片后端和视频链路；
- 构建源、生成物和历史资产边界；
- HTTP API、任务状态、幂等、会话、上传和配额；
- DreamAPI 精确请求合同、故障、热修和证据缺口；
- 测试分类、验证阶梯、正式发布和回滚；
- Windows/Linux 日常运维和排障；
- 凭据位置、安全问题、生产资源和数据连续性；
- 西雅图迁移结论；
- 明确的 P1/P2/P3 后续工作。

任何后续改动只有同时满足以下条件，才能宣称完成：

1. 用户需求逐项落地；
2. 权威源和生成物一致；
3. 相关测试从 RED 到 GREEN；
4. 验证强度与风险匹配，没有无意义重复；
5. Git diff 不含秘密、运行数据或无关改动；
6. 外部合同变化有最终媒体证据；
7. 正式发布通过标准入口并同时出现两个成功 marker；
8. 发布后健康、生产字节和用户关键路径核对通过；
9. 失败或未验证边界写清楚，不用“应该”“大概”冒充完成。
